# Better Reports — SDD progress ledger

Plan: docs/superpowers/plans/2026-09-07-better-reports.md
Spec: docs/superpowers/specs/2026-09-07-better-reports-design.md
Mode: subagent-driven, adapted (NO git in this tree — no commits, no worktrees; reviewers read changed files directly).
Baseline: 590 pytest tests green before Task 1. After Task 4: 594. A separate
Harish-report thread ran afterward and added +9 tests (real baseline 603).
Now: **604** (Task 5 landed).

## 2026-09-08 — PLAN COMPLETE (Tasks 1-16). Inline execution via executing-plans.
Suite 594 (after Task 4) → **625 passing**. Both PDF backends + compat renderer + landing
`npm run build` all green. RM demo set regenerated (md + 3 PDFs + 3 landing assets),
leak-free, with the plain-language layer.

Post-plan hardening applied to `plain_glossary.gloss_first_use` beyond the plan text:
`_is_structured_slot` skips headings, table rows, TOC / end-of-line entries, italic
blockquote notes (`> _…_`), and `- **bold-label:**` bullets so glosses only land in real
prose. `strip_source_citations` regex eats the preceding `[ \t]*`. `render_terms_section`
inserts a period before `note`.

## 2026-09-08 (round 2) — gloss engine hardened again during Harish regen
Regenerating a real deep report surfaced more `gloss_first_use` bugs (the plan flagged this
as the "trickiest code"). Fixes in `plain_glossary.py`:
- **word-boundary matching** (`_is_word_char` both sides) — "Monthly Luck" no longer fires
  inside "Monthly Lucky", stage Hanja no longer fires inside 사주 etc.
- **section-aware TOC skip** (`_nearest_heading` contains "contents")
- **parenthetical / slash-list / `*italic*` / `**bold**` span skips**
- **attributive-noun skip** — a `_MODIFIED_NOUNS` follow-word list (stem, table, period,
  ten-god, timing, presence, …) + preceding conjunction/preposition (and/or/without/with)
- `collect_used_terms` now reuses `_is_structured_slot` via `_first_real_use`, so the
  glossary lists only terms in real prose (Harish deep glossary 32 → 6 entries)
- dropped the `"hidden stems"` and (reworded) `"Day Master"` low-value inline triggers
- new regression test `test_gloss_does_not_corrupt_markup_or_split_phrases`. Suite 625 → 626.
- **RM demo set regenerated** (rm-*.md/pdf + demo-rm-*.pdf) with the fixed engine — the
  earlier Task-16 build had the TOC-corruption bug.

## 2026-09-08 (round 3) — Harish full regen + more gloss/strip/translation hardening
While re-grounding Harish's `career.md` / `land-workshop-business.md` in `knowledge/12-16`:
- `gloss_first_use` gained: `_nearest_heading` TOC skip, parenthetical/slash-list/`**`/`*` span
  skips, `_MODIFIED_NOUNS` attributive-noun skip + preceding conjunction/preposition skip,
  `_is_word_char` boundary on both sides. `collect_used_terms` now calls `_is_structured_slot`
  via `_first_real_use` (glossary lists only real prose uses). Dropped low-value inline triggers
  `hidden stems`; reworded the `일간` PlainDef to avoid a `_STEM_PROFILE` clash.
- `strip_source_citations`: `*(see\s…)*` (was `see ` with a hard space — broke on line-wrapped
  citations), new `_SOURCE_CITATION_NOSEE_RE` for `*(knowledge/…)*`, `_SOURCE_PATH_RE` now also
  matches the short `knowledge/13` form and a trailing ` §Section` clause. Internal `§N`
  document cross-refs are deliberately preserved.
- `saju_html.__init__` translation maps: +~20 compound Korean / Hanja terms (편인식신, 겁재奪財,
  재생관, 재다신약, 인성생신, 진상관/가상관, 재고, 택일, 풍수/風水, 양택/陽宅, 하도낙서/河圖洛書,
  황도길일, 손없는날, 건제십이신, 이사/移徙, 개업/開業, 상생, 상극).
- Regression test `test_gloss_does_not_corrupt_markup_or_split_phrases`. Suite 626.

## RESUMED 2026-09-08 — Track B COMPLETE (Tasks 5-8 + KB sweep), inline execution
Suite 604 → 610. Next: Track A (Tasks 9-16, plain_glossary.py plain-language layer).

## SESSION PAUSED 2026-09-07 — after Task 4 (implemented AND reviewed: Approved)

### RESUME STEPS
1. `cd /mnt/data2/git_repos/misc/saju && python3 -m pytest -q` — expect **594 passed**. If not, inspect
   `tests/test_knowledge_files.py` + `knowledge/15-health-and-body.md`.
2. Continue in order: **Task 5** (knowledge/16-date-selection.md) → **KB cleanup sweep** (one fix
   subagent, see CROSS-CUT below) → **Task 6** → **Task 7** → **Task 8** (end of Track B — summarise to
   user) → **Tasks 9-16** (Track A: plain_glossary.py + wiring + PDF safety + regen).
3. Per-task loop: regenerate task brief
   (`"$SKILL/scripts/task-brief" docs/superpowers/plans/2026-09-07-better-reports.md <N> <outfile>`,
   SKILL=/home/harish/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development)
   → dispatch implementer (general-purpose, sonnet, TDD, writes a report file, no git)
   → dispatch reviewer (general-purpose, sonnet, reads changed files — no diff file since no git)
   → fix subagent only for Critical/Important → mark COMPLETE here → next.
   Final whole-branch review on opus.

## Status
- Task 1 (knowledge/12-career-and-vocation.md): COMPLETE (review clean; 591 tests)
- Task 2 (knowledge/13-wealth-and-business.md): COMPLETE (review clean; 592 tests)
- Task 3 (knowledge/14-directions-and-relocation.md): COMPLETE (review clean; 593 tests)
- Task 4 (knowledge/15-health-and-body.md): COMPLETE (review Approved; 594 tests; Hanja rule met in full this time)
- Task 5 (knowledge/16-date-selection.md): COMPLETE (test added; 604 tests)
- KB cleanup sweep (see CROSS-CUT): COMPLETE 2026-09-08 — Hanja-on-first-use fixes in
  12/13/14/15/16; 13 재생관 attribution → knowledge/03 생성 cycle; 14:11 풍수 (風水) gloss,
  14 depersonalised the scope-boundary note, 14 dropped the awkward US-spelling parenthetical;
  12 "Skill-Levers" labelled modern practice; 15 형 gloss 刑=penalty (was "harm"), 15 방위/미 Hanja;
  05-ten-gods.md:125 겁재夺財 → 겁재奪財. Suite still 604.
- Task 6 (wire 12-16 into method/glossary/CLAUDE/skill/openwiki): COMPLETE 2026-09-08 —
  09-interpretation-method.md refs at Steps 4/5/8; 00-glossary.md +재고/재다신약/택일/개업/양택(excluded);
  CLAUDE.md "How to Answer" item 2 theme list; .claude/commands/saju.md step-4 list; openwiki file index rows 12-16.
  test_new_knowledge_files_are_referenced added; suite 605.
- Task 7 (re-ground report_data tables + test_knowledge_grounding.py): COMPLETE 2026-09-08 —
  added `# source:` comments above _ELEMENT_ORGANS/_ELEMENT_ASSOCIATIONS/_CAREER_DOMAINS/
  _GROUNDING_PRACTICES/_SIGNATURES in report_data.py; renamed 3 career titles to match kb12
  wording (Human Resource Development→People Development & Culture; Politics & Advocacy→
  Public Affairs & Advocacy; Counseling & Therapy→Mediation & Counselling); new
  tests/test_knowledge_grounding.py (4 tests, all green — no prose regressions).
- Task 8 (reconcile prose_fillers/prose_scaffold wording): COMPLETE 2026-09-08 —
  no wording drift to fix (Task 7 only renamed unasserted strings); added `# source:` /
  citation notes in prose_fillers.py + prose_scaffold.py _ELEMENT_ORGANS and
  depleted_element_health docstring; added test_health_filler_uses_kb15_organ_wording.
  Suite 610. <-- END OF TRACK B (Tasks 5-8 all done)
- Task 9 (plain_glossary.py + tests): COMPLETE 2026-09-08 — src/saju_engine/plain_glossary.py
  (PlainDef, PLAIN_GLOSSARY, gloss_first_use, collect_used_terms, render_terms_section) +
  tests/test_plain_glossary.py (6 tests). Suite 616. Not yet wired into any report generator.
- Task 10 (plain_words_* section-callout fillers): COMPLETE 2026-09-08 — 7 functions in
  prose_fillers.py (plain_words_day_master/_career/_relationships/_health/_timing/_pattern/
  _ten_gods) each returning a one-line "> **In plain words:** …" blockquote or ""; helper
  `_plain`; test_plain_words_fillers_return_blockquote. Suite 617. Not yet wired.
- Task 11 (wire Track A into premium_report.py): COMPLETE 2026-09-08 — imported plain_glossary;
  `_append_callout` helper; PF.plain_words_* callouts appended in day_master_portrait (day_master
  + ten_gods), career_wealth, relationships, health_vitality, natal_patterns, timing; final
  assembly runs gloss_first_use + appends render_terms_section after Closing Note. Hardened
  gloss_first_use: `_is_structured_slot` skips headings / table rows / `**bold label:**` and
  scans onward for a safe prose slot (fixed 3 Quick-Reference test breaks). render_terms_section
  adds a period before note. 3 new tests. Suite 620.
- Task 12 (wire Track A into skeleton.py): COMPLETE 2026-09-08 — prose_scaffold.generate_plain_words
  (delegates to prose_fillers.plain_words_* via dict ctx; 5 keys); skeleton.py imports it +
  plain_glossary, `_sec` helper appends callouts to scaffold sections 3-7, final doc runs
  gloss_first_use + render_terms_section("skeleton"). 2 new tests. Suite 622.
- Task 13 (wire Track A into compat_report.py): COMPLETE 2026-09-08 — `_plain` +
  `_plain_words_verdict/_daymaster_cross/_timing` helpers; callouts wired into basic
  (verdict + cross) and deep (verdict + cross + timing); generate_compat_report runs
  gloss_first_use + render_terms_section("compat"/"compat_deep"); added "compat"/"basic"
  to plain_glossary._SHORT_TIERS. 1 new test. Suite 623.
- Task 14 (PDF-layer safety): COMPLETE 2026-09-08 — `_SOURCE_CITATION_RE` now eats the
  preceding `[ \t]*` so removing `*(see …)*` leaves no dangling " ."; added
  test_strip_keeps_plain_words_callout_and_glossary. Built deep premium PDF (reportlab +
  HTML backends) and a deep compat PDF: no "see knowledge/" leak, callouts + glossary
  present, no residual Hanja (bar the pre-existing 命 cover watermark). Suite 624.
- Task 15 (combine_candidate_report.py glossary-last fix): COMPLETE 2026-09-08 —
  `_extract_terms_section` / `_strip_terms_section`; combine_report pulls the glossary out
  before the Closing Note extraction and re-appends it as the final block; `_strip_closing_note`
  regex now stops at a following `## What the Terms Mean`. 1 new test. Suite 625.
- Task 16 (regenerate demo/candidate outputs): COMPLETE 2026-09-08 — RM (Kim Nam-joon,
  1994-09-12 13:28 Seoul, 辛 Yin Metal, Water favourable) sample/essential/deep `.md`
  regenerated via the engine + PDFs rebuilt (rm-sample-report.pdf, rm-essential-report.pdf,
  rm-report.pdf) and copied to apps/landing-page/public/demo-rm-{sample,essential,deep}.pdf.
  All 6 PDFs: 0 "see knowledge/" leaks, 0 ENGINE DRAFT markers, callouts + glossary present
  (sample has neither, by design). Pillars unchanged (甲戌/癸酉/辛丑/甲午). Report date now
  2026-09-08. `npm run build` in apps/landing-page passes. vishnu-priya / mahesh not
  regenerated (spot-check only per plan).

## State of the tree (what's changed so far)
- Created: knowledge/12-career-and-vocation.md, 13-wealth-and-business.md, 14-directions-and-relocation.md,
  15-health-and-body.md
- Created: tests/test_knowledge_files.py (4 functions: test_12_ / test_13_ / test_14_ / test_15_)
- NO src/ changes. NO knowledge/ wiring (Task 6). report_data.py untouched (Task 7).
- Suite: 590 -> 594 passing.

## Minor findings (for final review triage)
- T1: knowledge/12 — 재성/관성/식상/인성/비겁/희신 used without inline Hanja on first use in-file (defined in 05). Retrofit in the KB sweep.
- T1: knowledge/12 "Skill-Levers by Dominant Ten-God" not labelled as modern practice (each lever aligns with 05 manifestation; Scope&Limits disclaims career advice).
- T1: tests/test_knowledge_files.py element-loop assert lacks a failure message.
- T2: knowledge/13:52-54 — summary sentence over-attributes 재생관 to 05's Conflict-Patterns table; the subsection itself correctly grounds it in 03's generating cycle. Reword attribution in the KB sweep.
- T2: knowledge/13:42 — "재성 혼잡" without (混雜) gloss on first use (not on required list).
- T3: knowledge/14:11 — 풍수 first use missing Hanja -> 풍수 (風水, Fengshui).
- T3: knowledge/14:157-158 — internal-ops sentence ("a recent hand-written land reading blurred this line") — depersonalise to a general caution.
- T3: knowledge/14:37 — awkward US-spelling parenthetical inserted only to satisfy a Task 7 substring check.
- T4: knowledge/15:92 — 방위 (方位) / 미 (味) correspondence-category terms without Hanja on first use.
- T4: knowledge/15:144 — 형 glossed as "(刑, harm)"; 刑 = punishment/penalty, "harm" is 해 (害). Fix the English gloss.
- T4: knowledge/15:33-37 — organ phrasings wrapped in **bold** + " / " spaced slashes. Task 7 must strip markup/spacing for a literal `_ELEMENT_ORGANS` match (flag to Task 7 implementer).
- PRE-EXISTING: knowledge/05-ten-gods.md:125 writes 겁재夺財 (simplified 夺); 12 and 13 use 겁재奪財 (traditional). Normalise 05 to 奪.

## CROSS-CUT — "KB cleanup sweep" (run as ONE fix subagent after Task 5, before Task 6)
Sweep knowledge/12,13,14,15,16 for:
- Full Hanja-on-first-use compliance for EVERY technical term (recurring partial misses: 대운, 월운, 충, 희신, 재성, 풍수, etc.)
- knowledge/13:52-54 재생관 attribution fix (credit knowledge/03 generating cycle, not 05's conflict-patterns table)
- knowledge/14:11 풍수 -> 풍수 (風水, Fengshui); knowledge/14:157-158 depersonalise; knowledge/14:37 drop the spelling parenthetical (but keep a "Centre"/"center" mention so Task 7's `"cent"` substring test still passes — OR adjust that test in Task 7)
- knowledge/12: label "Skill-Levers" as modern practice; add inline Hanja for the ten-god class terms
- knowledge/15:92 방위/미 Hanja; knowledge/15:144 fix 형 gloss ("刑, penalty" not "harm")
- knowledge/05-ten-gods.md:~125 normalise 夺 -> 奪
- Re-run `python3 -m pytest -q` after; expect still 594 (or +1 if Task 5 already landed).
- NOTE for Task 7: knowledge/15:33-37 organ strings are **bold** + spaced " / " — reconcile _ELEMENT_ORGANS
  by stripping markup, or write the KB table plain during the sweep.

---

## 2026-09-13 — NEW PLAN: 조후 (climate) cross-check for favorable-element engine

Plan: docs/superpowers/plans/2026-09-13-climate-favorable-element.md
Spec: docs/superpowers/specs/2026-09-13-climate-favorable-element-design.md
Mode: subagent-driven, adapted (NO git in this tree — no commits, no worktrees; reviewers read changed files directly instead of a diff file).
Baseline: 627 pytest tests green before Task 1.

### Status
- Task 1 (climate.py + tests/test_climate.py): pending
- Task 2 (yongsin.py merge logic + tests/test_yongsin_consistency.py): pending
- Task 3 (premium_report.py / compat_report.py supporting-element fix): pending
- Task 4 (knowledge/17-climate-method.md + knowledge/09 Step 3 update): pending
- Task 5 (regenerate affected candidate reports): pending
- Task 1 (climate.py + tests/test_climate.py): COMPLETE — review Approved. Suite 627 -> 640.
  Reviewer flagged one Important item: climate.py cites knowledge/17-climate-method.md, which
  does not exist yet — resolved by controller as a cross-task sequencing artifact (Task 4
  creates that exact file later in this same plan), not a real defect. No fix dispatched.
- PLAN AMENDED 2026-09-13: implementer for Task 2 surfaced a real bug not in the
  original spec (compat.py's 4 scoring functions + override mechanism read
  strength_assessment["candidate_favorable"] directly, bypassing yongsin.py's
  new climate merge -> scoring/display mismatch + override silently defeated
  for balanced+hot/cold charts, e.g. Mahesh, the canonical demo compat pair).
  Presented to user; decision: full fix now. Inserted Task 2b into the plan
  (compat.py fix) and expanded Task 5 to cover the two compat report pairs
  (pawan_sruthi, harish_vinothini). Plan file updated in place.
- Task 2 (yongsin.py merge logic + tests/test_yongsin_consistency.py): COMPLETE — review Approved
  (verbatim brief match, all behaviors verified independently). 3 known/expected failures remain in the
  tree pending Task 2b (test_override_flows_into_compat_display) and Task 3 (2x test_premium_report.py) —
  reviewer confirmed root causes are in compat.py/premium_report.py, not yongsin.py.
- Task 2b (compat.py scoring + override routed through favorable_element(); yongsin.py gains
  reader_override_favorable precedence): COMPLETE — review Approved. Reviewer independently
  recomputed the _MA_VP_SCORES anchor update (yongshin 6->9, combined_elements 2->4) via live
  engine execution and confirmed it's mathematically correct, not a fixture papering over a bug.
  Suite: 2 failed (both expected/pending Task 3), 647 passed.
- Task 3 (premium_report.py + compat_report.py supporting-element fix): COMPLETE — review
  Approved. Reviewer independently traced the "no other client-facing gap" claim through
  cli.py/combine_candidate_report.py and confirmed skeleton.py/prose_scaffold.py are genuinely
  dev-only, not client-facing. Full suite: 649 passed, 0 failed. All code tasks (1, 2, 2b, 3)
  now complete and green.
- Task 4 (knowledge/17-climate-method.md + knowledge/09 Step 3 update): COMPLETE — review
  Approved. Reviewer cross-checked the knowledge file's band table and conflict-resolution rule
  word-for-word against climate.py and yongsin.py (auditability check) - matches exactly. Only
  trivial wording nitpicks noted, no fix needed.
- Task 5 (regenerate affected reports): implementer found blast radius smaller than expected
  (Sruthi/Pawan/Gurumoorthy/Mahesh use reader-argued overrides that bypass climate entirely by
  design -- correctly left untouched; only Harish + Vishnu Priya + pawan_sruthi + harish_vinothini
  actually needed regen). Reviewer verified Harish's base report is now fully internally
  consistent (Water/Metal throughout). Reviewer found ONE Critical issue: the pawan_sruthi deep
  compat file's Day Master Snapshot "fix" (Pawan supporting Water->Metal, Sruthi Earth->Fire) now
  CONTRADICTS their own published natal reports (which still show duplicated Water/Water,
  Earth/Earth) -- needs a decision, not yet resolved. Also surfaced 2 pre-existing bugs (out of
  scope, flagged not fixed): premium_report.py's "This Year Means" line uses raw unresolved
  favorable element (live-wrong in Sruthi's/Pawan's published reports); Harish's 3 follow-up
  files (career.md, land-workshop-business.md, luck-timeline.md) still argue old Fire/Wood, now
  stale vs corrected base report. STATUS: presenting all of this to user for decisions before
  closing out the plan.
- Follow-up fix (controller-applied, user-approved): corrected Pawan's and Sruthi's natal
  reports (pawan-report.md, sruthi-report.md) to match the classically-correct supporting
  element the compat regen exposed -- Pawan Supporting Water->Metal (+direction north->west),
  Sruthi Supporting Earth->Fire (+direction center->south), 5 lines each file. Both PDFs
  rebuilt via build-pdf.sh. Verified no stale duplicate references remain (grep clean). Full
  suite re-confirmed green: 649 passed.
- PLAN COMPLETE 2026-09-13. All 6 tasks (1, 2, 2b, 3, 4, 5) done and reviewed; one Critical
  cross-deliverable finding from Task 5's review resolved per user decision. Remaining known,
  explicitly-flagged-not-fixed items for a future session: (a) premium_report.py's "What This
  Year Means for You" line reads raw unresolved favorable element instead of favorable_element()
  -- live-wrong in Sruthi's/Pawan's "This Year" sentences; (b) Harish's career.md/
  land-workshop-business.md/luck-timeline.md still argue old Fire/Wood, stale vs corrected base
  report -- flagged in candidates_horoscope/README.md; (c) harish_vinothini compat pair
  reconstructed Vinothini's chart from published pillars (her real birth data not recoverable
  from repo) -- qualitatively correct but not a from-scratch regen; pair also missing from
  README's compat index (pre-existing gap).

## 2026-09-13 (session close)

Same-day follow-on work after the plan closed: created a new compat pair
`candidates_horoscope/marriage_compatibility/harish_manvitha/` (both tiers + PDFs; added to
README's compat index) as a fresh, no-override validation of the shipped engine — Harish
(Water, unchanged) x Manvitha (甲, strong, 亥월 -> Fire/Earth, freshly computed). Composite
70/100, Strong. Found and worked around one more pre-existing tooling bug:
`md_to_saju_compat_pdf.py`'s default `--output` path resolves incorrectly when omitted
(wrote into a stray `src/candidates_horoscope/...`); always pass `--output` explicitly.

Full day's work (this plan + the Pawan/Sruthi natal reconciliation fix + harish_manvitha) is
now also recorded in `docs/MEMORY.md` (project context file) and the cross-session auto-memory
at `~/.claude/projects/-mnt-data2-git-repos-misc-saju/memory/` (new files:
climate-favorable-element-2026-09.md, user-feedback-scope-decisions.md).

3 items remain explicitly flagged, not fixed: premium_report.py's "What This Year Means" line
(wrong raw field), Harish's career.md/land-workshop-business.md/luck-timeline.md (stale
Fire/Wood), md_to_saju_compat_pdf.py output-path bug (has a workaround).

## 2026-09-13 — NEW PLAN: Engine Validation Campaign Plan 1 (foundation + W1 pillars)

Plan: docs/superpowers/plans/2026-09-13-engine-validation-plan-1-foundation-pillars.md
Spec: docs/superpowers/specs/2026-09-13-engine-validation-campaign-design.md
Mode: subagent-driven, adapted (NO git — no commits; each task's gate is full pytest suite;
reviewers read changed files directly).
Baseline: 649 pytest tests green before Task 1.

### Status
- Task 1 (fixture seed + loader): complete (review clean, suite 652)
  - Minors deferred to final review: unused `import Any` (validation.py:14) and unused `pathlib` import (test_loader.py:5) — drop when Task 2 touches these files; URL-policy inconsistency (주간경향 lee-myung-bak entry has path but url:""); two header-sourced citation texts not strictly verbatim (Roh 김재원 date suffix appended, Kim Young-sam 격국용신정해 parenthetical dropped) — disclosed, immaterial to traceability
  - Plan-code gap resolved at Task-2 dispatch: check_pillars must skip expected fields that are null/None (kim-dae-jung has expected.pillars.hour: null) + unit test
- Task 2 (check engine + W1 harness): in_progress
- Task 3 (edge-stratified acquisition): in_progress
- Task 4 (report runner + certification): pending
Task 3 (partial, first attempt stopped by user):
- Amendment DONE: _ALLOWED_INPUT extended (city/early_zi_time/korean_yazi/n_periods), unknown keys raise ValueError; 2 unit tests in test_loader.py (verified controller-side; suite 659 → 662 total incl. 1 new harness entry).
- ENGINE BUG #1 (W1, hour-boundary-2340, FAIL kept as expect_match): Korean 야자시 hour stem — two independent sites (forceteller, manseryeok.com, both 야자시 mode) give 庚子 (五鼠遁 from NEXT day's 일간 壬, day pillar retained 辛亥); engine gives 戊子 (五鼠遁 from current-day 일간 辛). Root cause: src/saju_engine/pillars.py::_hour_stem_day_stem (L185-195) returns raw["day_stem"] unconditionally. Engine hybrid matches neither convention (조자시 advances day pillar to 壬子). LOGGED, NOT FIXED. Note for fix campaign: must not regress the earlier solar-rollback correction the function comments describe.
- User stopped first implementer: browser automation looping (repeated same form steps on forceteller). Root cause: multi-step confirm flow + no retry cap. Fix: anti-loop protocol (manseryeok.com GET-URL reuse, 2-strike retry cap, per-site budget, plan-then-acquire) mandated in re-dispatch.
- Task 3 re-dispatched (second attempt) for remaining categories 2-6. Gate for this task: 661 passing + 1 known FAIL, nothing else.
- **ENGINE BUG #1 FIXED 2026-09-13 (user directive — overrides campaign's log-don't-fix for this bug):**
  `src/saju_engine/pillars.py::_hour_stem_day_stem` now takes `(raw, eff_hour, convention)` and returns
  the NEXT day's day-stem when `eff_hour == 23 and convention == "korean"` (야자시). Chinese path untouched
  (sajupy already advances the day pillar there). Post-fix, both conventions agree on the hour stem
  (next-day 일간 at hour 23) and differ only in whether the day pillar advances. Fix verified against the
  2-site ground truth (forceteller + manseryeok.com 야자시 mode → 庚子 from 壬, 일주 辛亥 retained) via the
  fixture hour-boundary-2340, which flipped from the deliberate FAIL to PASS. Regression tests updated:
  test_engine.py (Pawan 庚子, Mahesh 戊子), test_cross_validate.py (Pawan, Mahesh, "Solar moves into Zi
  Korean" 甲子, Honolulu 壬子), test_pillars.py ("Korean into Zi" 甲子, Honolulu 壬子, NEW
  test_korean_yazi_hour_stem_asymmetry covering the 23:40 야자시 vs 00:15 조자시 asymmetry — the 00:15 arm
  runs with use_solar_time=False because Seoul's solar correction legitimately rolls 00:15 back to 23:43 of
  the previous day, which is separate correct 야자시 behavior, not a bug). Full suite: **663 passed, 0
  failed** (was 662 = 661 pass + 1 known FAIL; +1 = the new regression test). Task 4's gate is now fully
  green instead of "1 known FAIL open".
  ⚠️ Client-impact note: any existing client chart born 23:00–23:59 (야자시) has a wrong 시주 천간 in its
  published report and needs regeneration; check candidates_horoscope/ for affected charts.
  **Affected client deliverables (grep-verified 2026-09-13, four-pillar-table occurrences):**
  - **Pawan** 1991-10-03 23:45 — published 시주 戊子, correct **庚子** (십신 hour-stem flips 食神→편재 vs 丙 일간).
    Files: candidates_horoscope/reports/pawan/pawan-report.md:143,145,162 + career.md:7,137 + pawan-report-legacy.md;
    pawan-report.pdf + career.pdf need rebuilds; pawan_sruthi compat pair (basic+deep .md, PDFs).
  - **Mahesh** 1995-01-19 23:50 — published 시주 丙子, correct **戊子** (kills the published 정편관혼잡
    diagnosis: hour-stem 丙 편관 vanishes, becomes 戊 편인 — material 격국 change). Files:
    candidates_horoscope/reports/mahesh/relationships.md:9, career.md:9, mahesh-combined.md:232,373;
    mahesh-report.pdf / mahesh-combined.pdf / mahesh-combined-html.pdf need rebuilds.
  - NOT affected: rm (13:28 未시), Gurumoorthy (8:30), Sruthi (2:45), Harish (3:10), harish_manvitha
    (21:15) — their 戊子/丙子 grep hits are 대운/세운 tables, not hour pillars.
  Regeneration is OUT of the validation campaign's scope (interpretive rework, not a mechanical fix);
  flagged for a dedicated follow-up after the campaign closes or per user direction.

## Task 3: implementation verified + review (2026-09-13)
- Controller independently verified: full suite 668/0; 16 fixtures; 11 evidence PNGs; ground-truth values match 2-site evidence.
- Task-3 review agent: **spec ✅, quality Approved**, 2 Important + 3 Minor findings.
  - Finding 1 (Important): category-1 missing its 00:15 sub-seed (brief named it) → FIX SUBAGENT DISPATCHED (fixture `hour-boundary-0015`, 1990-06-15 00:15 Seoul korean use_solar_time=false, 2 sources + evidence).
  - Finding 2 (Important, category-6 provenance): RATIFIED by controller — buyiju satisfies the brief's mainland-source requirement; sajumania-in-조자시-mode is the second independent source per the campaign constraint.
  - Finding 3 (Minor, DST entry expect_match vs brief's suggested documented_interpretation): RATIFIED — utc_offset pinned in input ⇒ no cross-tool disagreement exists; notes carry the rationale.
  - Minors recorded for final review triage: sajumania screenshots are 291×276 element crops (pillars legible); sajumania citations use bare homepage URL (accepted, consistent with existing entries).
- Reviewer's "⚠ cannot verify" items: no git so implementer file-touch attribution rests on mtimes + report; nothing contradicts the no-engine-change claim (pillars.py change is controller-attributed, user-directed).
- Loader test count 11 vs 12: non-issue — the 12 belongs to Task 1's scope, Task-3 brief never mentions it.
- Fix verified independently (suite 669/0, entry + evidence inspected) and re-review returned: Finding 1 CLOSED (expected pillars independently recomputed and confirmed; 2 sources visually verified), no new blocking findings. Final Task-3 verdict: spec ✅, quality Approved.
- Task 3: complete (review clean after 1 fix loop).
- Reviewer advisory minors (recorded, non-blocking): buyiju POST-derived bare tool URL (accepted convention, 2 entries); hour-boundary-0015 is the only use_solar_time:false entry — deliberate, documented in notes.

## Task 4: implementation verified + review (2026-09-13)
- Controller independently verified: full suite 672/0; CLI exit 0 `checks: 17 (PASS 17, INTERPRETATION 0, FAIL 0)`; certification epilogue inside render_report (idempotent — CLI re-run byte-identical).
- Task-4 review agent: **spec ✅ (code verbatim vs brief), quality Approved.**
- Minor (recorded for final review): epilogue's "W1 Pillars: CERTIFIED / Suite: 672 / 2026-09-13" is a frozen snapshot — after Plans 2–6 add tests, regenerated reports re-emit it. Suggested: dynamic count, "(as of 2026-09-13, W1 gate)" qualifier, or Plan-2 task 1 updates the epilogue.
- Informational: `_CHECKERS` registry currently unused — brief-mandated verbatim; leave, prune if Plans 2–6 never consume it.
- **W1 CERTIFIED. Gate: Plan 2 (W2 대운/세운) may start.**
- Task 4: complete (review clean).

## Final whole-plan review (2026-09-13, opus) → report /tmp/sdd-val1/final-plan1-review.md
- **Verdict: Ready for Plan 2 — YES.** Verified live: 672/0; CLI 17 PASS/0 FAIL exit 0; regenerated report byte-identical (idempotence independently confirmed).
- Severity: 0 Critical, 2 Important, 7 Minor.
  - Important 1: pillars.json hour-boundary-2340 notes still narrate the closed engine FAIL → fix before Plan 2.
  - Important 2: epilogue hardcodes "Suite: 672 / 2026-09-13" → data-driven fix (taken NOW rather than deferring to Plan 2 Task 1).
  - 야자시 fix verdict: correct & minimal (one fn, one call site); solar-rollback interaction safe; residual coverage gap (solar pushing 23:xx past midnight) = future edge seed.
- Minor triage: ACCEPT (1) sajumania crops, (2) bare URLs, (4) _CHECKERS unused (prune at campaign close if never consumed), (5) 0015 use_solar_time:false, (6) buyiju POST URLs. FIX-BEFORE-PLAN-2: (3) epilogue staleness.
- Additional Minors logged for Plan 2/campaign close: jo-arm assert missing in test_korean_yazi_hour_stem_asymmetry (folded into fix wave), asymmetric typo guards on expected keys, loader root-type/subsystem-filename validation, "W1 — pillars" hardcoded heading prefix, _ALLOWED_INPUT lacks reference_* keys (Plan-2 extension point), CLI lacks mkdir for docs/audits/.
- Fix wave: ONE fix subagent dispatched (sonnet) with brief /tmp/sdd-val1/final-fix-brief.md → report /tmp/sdd-val1/final-fix-report.md. Scope: the 2 Important + jo-arm assert.

## 2026-09-13 late — Final review fix wave CLOSED (W1 gate formally closes)
Fix subagent (sonnet) applied all 3 findings from final-plan1-review: Fix 1 fixture note closure record, Fix 2 data-driven epilogue in render_report (derived totals; stale "672 passing"/bare Date lines removed), Fix 3 jo-arm assert in test_korean_yazi_hour_stem_asymmetry.
Controller INDEPENDENT verification (not subagent claims): full suite 672 passed/0 failed; CLI `checks: 17 (PASS 17, INTERPRETATION 0, FAIL 0)` exit 0; report ends `Validation checks at W1 gate: 17/17 PASS.`; two CLI runs byte-identical (md5 ac42aeea1978eaf0473d0385896514a2); /tmp/sdd-val1/final-fix-report.md complete.
**W1 CERTIFIED — gate closed. Plan 2 (W2 대운/세운) may start.**
Next: write docs/superpowers/plans/2026-09-13-engine-validation-plan-2-daeun-sewoon.md (writing-plans skill), execute subagent-driven continuous mode. Minor carried: `_CHECKERS` unused until Plan 2 consumes it.

## Plan 2 (W2 대운/세운) — started 2026-09-13
Plan doc: docs/superpowers/plans/2026-09-13-engine-validation-plan-2-daeun-sewoon.md (written; self-reviewed). Execution: subagent-driven continuous (user's standing choice); briefs/reports /tmp/sdd-val2/. W1 gate formally closed before execution began (spec precondition satisfied).
Plan 2 Task 1: complete (daeun.json 7 entries, check_daeun/run_daeun_fixture, test_val_daeun.py, 7 textbook tests migrated; suite 672/0)
Task 1: complete (daeun.json 7 entries + check_daeun/run_daeun_fixture + test_val_daeun.py + registry [pillars,daeun]; 7 daeun tests migrated out of test_textbook_cases.py; suite 672/0; review Approved, 3 Informational for final-review triage; :156 대운 comment classified as retained Kim Young-sam source block, not orphaned)
Plan 2 Task 2: complete (sewoon.json 4 non-circular seed entries, check_sewoon/run_sewoon_fixture, _ALLOWED_INPUT reference keys, registry sewoon; suite 676/0)
Task 2: complete (sewoon.json 4 non-circular entries + check_sewoon/run_sewoon_fixture + _ALLOWED_INPUT reference_* keys + test_val_sewoon.py + registry [pillars,daeun,sewoon]; suite 676/0; review Approved, 0 findings above Informational)
Task 3: complete (inline controller execution; daeun.json +4 = 11 entries [park-geun-hye@1 순행 음년여, kim-tae-hee@7 역행 양년여, moon-jae-in@3, kim-yeon-a@9 역행 양년여] + sewoon.json +4 = 8 entries [입춘 pair 丙子/丁丑 1997-02-03/05, 절입 pair 丙午/丁未 1992-07-06/08]; all 8 two-source confirmed — manseryeok 4/4 budget + fourpillars.net GETs; 8 new evidence PNGs; manseryeok has NO calendar pages → pairs confirmed via fourpillars GETs instead; suite 684/0; report /tmp/sdd-val2/task-3-report.md)
Task 3 review: Approved (0 Critical / 0 Important / 4 Minor for final-review triage — 절입 minutes drift vs engine values, evidence-dir count 29 not 31 [fixed in report], ipchun pair single-site second source [documented deviation], brace-shorthand evidence paths). Controller independently verified: pytest 684/0, review file present, evidence count 29.
Plan 2 Task 4: complete (report W2 headings via _WORKSTREAM map, _GATE_HISTORY epilogue with frozen per-gate facts + live current-status line, CLI docs/audits mkdir; W2 CERTIFIED — suite 684/0, CLI 36 PASS/0 FAIL exit 0, idempotent md5 2e71a9a6…; W2 checklist: 음년여 순행 + 양년여 역행, 대운수 1–3 & 7–9 brackets, 입춘 pair, 절입 pair, 36/36 PASS)
Task 4 review: Approved (0 Critical / 0 Important / 0 Minor / 3 Informational). Controller independently verified: review file present, 0 ❌, pytest 684/0 and CLI md5 2e71a9a6… re-run by controller earlier post-final-edit.
Plan 2 whole-plan final review: READY (0 Critical/0 Important/6 Minor/4 Informational; controller independently verified: suite 684/0, CLI idempotent md5 2e71a9a6…, M1/M2 note typos confirmed real & note-only, counts 19/29 match). Triage: M1-M2 DEFER (note-only), M4 DEFER to campaign close, M3/M5/M6 ACCEPTED. Plan 2 CLOSED — W2 CERTIFIED.
Plan 3 T1: lookups harness (check_lookups/run_lookups_fixture, registry [pillars,daeun,sewoon,lookups]) + 10 십신 fixtures; harness 10 pass + coverage test intentionally pending T2/T3; full suite 694 passed / 1 known-intermediate failure (test_coverage_exhaustive per plan T1 Step 5 correction); CLI checks: 46.
Plan 3 T2: +10 12운성 fixtures (all 120 stem-branch cells, 10 DMs × 12 branches); harness 20 pass + 1 known-intermediate fail (coverage test, nayin/xunkong sections pending T3); full suite 704 passed / 1 known-intermediate failure (plan text says 704/0 — same documented T1 inconsistency); CLI checks: 56.
Plan 3 T3: +30 nayin (60 jiazi + 30 elements) +6 xunkong (6 旬 blocks) fixtures; corpus complete at 56; harness 57/57 (coverage test green); full suite 741 passed / 0 failed (plan text said 740 — it omitted the coverage test itself from its arithmetic); CLI checks: 92.
Plan 3 T4: external cross-check done (≥2 refs/family, all 4 families clean, 0 discrepancies), external {citation,url} refs appended to all 56 lookups fixtures, M1/M2/M4 daeun.json deferrals closed (note-only), W3 CERTIFIED (56/56), _GATE_HISTORY W3 row appended, Plan-4 gate line set. Gates: harness 107 passed, suite 741/0, CLI 92 PASS exit 0 ×2 (md5 52d2b2947bc6139f289201319897cc3f identical). Report: /tmp/sdd-val3/task-4-report.md
Plan 3 T5: whole-plan review READY (/tmp/sdd-val3/plan-3-final-review.md) — coverage table all ✅, 0 Critical/Important/Minor open (5 Informational documented), independent re-verification green (suite 741/0, CLI ×2 exit 0 md5 52d2b2947bc6139f289201319897cc3f, fixture counts 17+19+56=92 match _GATE_HISTORY, read-only surfaces untouched per mtime). Plan 3 CLOSED. W3 CERTIFIED. Gate passes: Plan 4 (W4 yongsin) may start.
Plan 4 T1: complete (yongsin harness + 5 layer-A fixtures; 748/0; CLI 97 PASS; report /tmp/sdd-val4/task-1-report.md; deviation: coverage-test self-escalation)
Plan 4 T2: complete (8 corpus+override entries; 13 total; harness 12 passed+3 xfailed; suite 753/3xf/0; CLI 105 (102/3/0) 0 FAIL; probe matched grounding verbatim; report /tmp/sdd-val4/task-2-report.md)
Plan 4 T3: complete (docs/research/2026-09-validation-yongsin.md — 6 external sources quoted/cited [4 with verbatim quotes], 7 corpus verdicts tabulated incl. 3 documented divergences; no source contradicts engine merge precedence; full 120-cell 궁통보감 table + 辰戌丑未 banding + 종격 logged as gaps, not fixed).
Plan 4 T4: complete (external cross-check: 3 rule families × ≥2 sources, no contradiction; W4 CERTIFIED — _GATE_HISTORY row + gate line Plan 5; report W4 section; CLI 105 checks PASS 102/INTERP 3/FAIL 0, md5 1d3f81ccc699bd9abdbeba1531bc2752 idempotent).
Plan 4 (W4 yongsin) FULLY CLOSED — W4 CERTIFIED 2026-09-13. Suite 753/0; CLI 105 checks (PASS 102, INTERPRETATION 3, FAIL 0) idempotent md5 1d3f81ccc699bd9abdbeba1531bc2752. Next: author Plan 5 (W5 climate) — gate passed.
Plan 5 T1: complete (climate subsystem registered — 16 band probes incl. 12-branch sweep + 4 defensive edges, registry wired, dual guard exact-key/unknown-key via probe discriminator; suite 772/3xf (plan predicted 773 — its "4 coverage tests" miscount, 3 authored, single explained cause); CLI 121 checks PASS 118/INTERP 3/FAIL 0 exit 0 matching plan exactly; report /tmp/sdd-val5/task-1-report.md).
Plan 5 T2: complete (climate 30 checks = 16 band + 14 merge; all 9 band x verdict cells covered + both climate_agrees polarities; 30 PASS / 0 INTERPRETATION — FINDING 1: the 3 documented_interpretation rows assert the ENGINE's values (Fire/Wood/Fire) so they match and PASS; INTERPRETATION needs a mismatch (validation.py:74-79). Step 2 JSON is the frozen artifact, Step 5/7 prediction was wrong. Fixture unchanged, resolution justified in report; T5 gate row must read passed=30 and CLI PASS 132. FINDING 2: module 36 (not 23) / suite 789 (not 776) — plan arithmetic forgot parametrize grows with the fixture 16->30. CLI 135 checks PASS 132/INTERP 3/FAIL 0 exit 0; report /tmp/sdd-val5/task-2-report.md).
Plan 5 T3: complete (research doc docs/research/2026-09-validation-climate.md — 402 lines, 10 numbered sources w/ URLs [20 URL refs, 7 domains; 4 domains inside §2-§5], citation bar green: grep -c http = 20, ≥3 domains in §2-§5 = 4, UNCERTAIN/120-cell/궁통보감/窮通寶鑑 = 16 hits). 辰戌丑未 [UNCERTAIN] resolved as the classical 寒暖燥濕 four-way reading (亥子 寒→火, 巳午 熱→水, 戌未 燥→水, 辰丑 濕→火) — REVERSAL of the plan template: the simplification is material for 辰·戌 (engine temperate/None vs source 辰→Fire/戌→Water), NOT 丑·未; engine agrees on all 10 branches where it prescribes at all, so there is no element-level disagreement in the subsystem, and 丑/未 agree by convergent validity (source pairs 辰丑/戌未 by damp-dry, engine pairs 辰戌/丑未 by quartet — both reach Fire/Water). Also logged: knowledge/17-climate-method.md:51-52's "辰戌 dry / 丑未 damp" aside is inverted for 辰 and 未 (wording defect, not engine behaviour; not fixed). 120-cell gap restated (§4) with a provenance warning — no source states a cell count; stem-by-month organ attributed to OpenFate, the 120-cell figure and the 欄江網/余春台/徐樂吾 genealogy to cantian as derived. 조후-vs-억부 priority finding recorded for campaign close (§5) — the "조후 outranks 억부 unconditionally" premise is UNSUPPORTED ("조후와 부억에는 명식을 떠난 고정 순서가 없습니다"), so the engine's rule is one defensible school position, not a deviation; the narrower real finding is that sources gate on climate extremeness while the engine gates on the strength verdict (engine's gate is narrower) — engine UNCHANGED, flagged as a campaign-close product decision. §6 records the measured 30 PASS / 0 INTERPRETATION (not the template's 27/3) per T2 Finding 1. Docs-only no-op holds: suite/CLI unchanged at 789 passed/3 xfailed and 135 checks PASS 132/INTERP 3/FAIL 0 exit 0. Third instance of the authoring-time miscount class (plan's Step 1 lists 4 억부-override rows, measured 5 — gurumoorthy-published omitted; deliverable correct). Report: /tmp/sdd-val5/task-3-report.md
Plan 5 T4: complete (known-bug regression lock — 2 non-strict xfails pin premium_report.py _right_now_callout ~L181 + ctx_favorable_phrase ~L1395 reading raw candidate_favorable; fix NOT applied per spec Out of Scope; divergence test passes; premium_report.py untouched (grep count exactly 2, both pre-existing); suite 791 passed/5 xfailed; CLI unchanged 135 checks PASS 132/INTERP 3/FAIL 0 exit 0). Step 1 defect reproduced under frozen reference date 2026-09-13: raw Fire / resolved Water (climate-balanced) / phrase 'fire energy' / callout tail **Fire** / DEFECT True. Four tests added: two passing guards (test_regression_input_keys_are_allowed — chart expressible via _ALLOWED_INPUT; test_resolved_fe_differs_from_raw_candidate — guards the lock's PREMISE so the xfails cannot become vacuous) + two xfail(strict=False) locks (callout must contain **Water**, has **Fire**; phrase must be 'water energy', is 'fire energy'). strict=False is deliberate: a later spec-sanctioned fix turns these into XPASS (the delete-the-marker signal) instead of failing the build. CLI unmoved BY DESIGN — the lock lives in pytest, not in climate.json, and the CLI counts fixture entries; the plan's forward prediction at line 110 ("3 + 2 = 5 xfailed") confirmed exactly. Report: /tmp/sdd-val5/task-4-report.md
Plan 5 T5: complete (W5 certification, inline). render_report climate prose block inserted after the yongsin block with all four correction classes applied (5 억부-override rows incl. gurumoorthy-published; the measured 5/4/3/2 split replacing the plan's miscounted "5 where 조후 decides"; guard-not-verdict framing so the 3 guarded rows render PASS and W5 contributes 0 INTERPRETATION; ctx_favorable_phrase ~L1395; 辰/戌 divergence with the 丑/未 convergent-validity explanation). _GATE_HISTORY W5 row = checks 30 / passed 30 (plan's passed 27 refuted by T2 Finding 1) with corrected note. Gate line flipped to "Plan 6 (W6 compat / spouse + career) may start only on W5 CERTIFIED." Report regenerated: scorecard `| climate | 30 | 30 | 0 | 0 |` L14, W5 prose L214, CERTIFIED line L283, `Current validation checks: 132/135 PASS across 6 subsystems` L284, gate L285. Whole-plan final review READY (/tmp/sdd-val5/plan-5-final-review.md) — coverage table all ✅, 0 Critical/0 Important, 11 Minor/Informational documented (M2 = the documented_interpretation guard≠verdict finding that drove the numeric corrections; M6 = the 조후-vs-억부 gate-width reversal, campaign-close product decision; M8 = knowledge/17-climate-method.md:51-52 wording defect, not fixed). Certification checklist 9/9 green with box 2 rewritten. Independent re-verification at review time: census 30 (16 band/14 merge; 27 expect_match/3 documented_interpretation; renders 30 PASS), gate row 30/30 MATCHes fixture-derived, read-only mtimes all predate 2026-09-14 (only validation.py carries today), grep -c candidate_favorable = 2 at L181/L1395, 30/30 sources non-empty + 14/14 merge rows cited, 7 URL domains, suite 791 passed/5 xfailed/0 failed, CLI md5 b2ca53714fcaf20107c63ac42465cf76 identical across THREE runs.
Plan 5 (W5 climate) FULLY CLOSED — W5 CERTIFIED 2026-09-13. Suite 791 passed/0 failed (5 xfailed: 3 yongsin documented_interpretation + 2 known-bug locks); CLI 135 checks (PASS 132, INTERPRETATION 3, FAIL 0) idempotent md5 b2ca53714fcaf20107c63ac42465cf76. Gate line flipped to Plan 6 (W6 compat). Next: author Plan 6 (W6 compat / spouse + career) — gate passed.
Plan 6 T1: complete (compat subsystem registered — 17 layer-A fixtures = 10 band probes on the chart-free 4-band ladder [all three boundaries 45/65/80 straddled + 2 out-of-range defensive edges] + 7 subsystem probes on the published harish x manvitha pillars, exactly A6's pillars-only-reproducible set, the 4 out-of-scope subsystems [yongshin/ilju_pair/combined_elements/daeun_sync] declared in every entry's notes; all 17 expect_match. _CHECKERS wired for the first time — 7/7 across _SUBSYSTEM_ORDER/_WORKSTREAM/_RUNNERS/_CHECKERS — via renaming the registered band probe check_compat_band→check_compat to match the plan's name at L270/L277/L348/L485/L811 [W5 precedent: the registered checker is unsuffixed, the deliberately-unregistered probes stay suffixed, so the name encodes registry membership]. Behaviour-neutrality PROVEN, not assumed: the CLI stdout md5 is byte-identical to the pre-rename baseline, confirming plan L261's finding that _CHECKERS is dead code [collect_results reads _RUNNERS only] — pruning still deferred to campaign close. Suite 824 passed/13 xfailed/0 failed; plan Step 9 predicted "5 + 5 xfailed" and is WRONG — the count is 13 because the prediction omitted the 3 육합 locks authored in Step 6 [5 W5 + 3 육합 + 5 C9] — 4th instance of the authoring-time miscount class. CLI 152 checks (PASS 149, INTERPRETATION 3, FAIL 0) exit 0, stdout md5 a22f946613ef38901403ad13e4592cfe identical across two runs; CLI arithmetic 132+17=149 PASS ✓; INTERPRETATION stays 3 [W4's]. NEW FINDING beyond the spec's 3 known bugs: the 육합 defect — lookup.SIX_COMBINATIONS is List[Tuple[str,str,str]] but compat._branch_pair_lookup tests 2-tuple membership, so the 육합 branch is unreachable dead code at compat.py:310 [+4], :407 [spouse-palace +20, the engine's own narrative calls it "the strongest favorable indicator in classical 궁합" yet reports the pair as having "no direct 합/충" — a false statement in a paid deliverable], :1295 [띠 +3], and :428 [반합 suppression, vacuous — LATENT ONLY]. Inverted-result nuance: 寅亥 and 巳申 are simultaneously 六合 and 六破, so they score −1 [a penalty where classical 궁합 prizes a combination] rather than 0. Logged, NOT fixed per the validation-only constraint; 3 non-strict xfail locks + 3 passing guards, guards written against behaviour a sanctioned fix will NOT change [guard arities prove the defect is table-specific → a bounded one-line fix; guard 3 asserts the inverse premise so it fails exactly when the behaviour changes]. Lock-kind verified non-vacuous: all 5 C9 params fail as genuine AssertionError with the intended message, not exceptions. Fix-simulation: 육합-only flips 0/5 C9 locks, C9-only flips 5/5, both flip 4/5 because 巳申 is simultaneously 六合 and 六破 — the entanglement is documented in a code comment so whoever lands both fixes expects 4 XPASS + one permanently-xfailing 巳申 row and decides the 巳申 priority on classical rather than test grounds. C9 itself [삼형 shadowing: 삼형 sits below 충/해/파 in the priority chain] is a DIFFERENT defect from 육합 and the two are independent. Divergence recorded: Step 10's "flip the Plan 6 checkbox in .superpowers/sdd/progress.md" names a convention this repo does not use [ledger grep -c '- [x]' = 0; plan docs 0 checked of 46 and 0 of 52] — prose line appended instead, per the ledger's actual convention. Also recorded: the multi-probe design [check_compat registered + check_compat_subsystem/check_compat_score dispatched by run_compat_fixture, following climate's precedent] vs the plan's single check_compat, and why the dispatcher raises on an unknown subsystem name by design [the 3 non-fixture invariants — 삼형 reachability, WEIGHT 112/100 reconciliation, the 육합 lock — live as module tests rather than widening the dispatcher and trading a real guard for a cosmetic fixture count]. Carried non-blocking: harish's longitude is 76.33 in sewoon.json but 79.42 in yongsin/climate/plan anchors. Report /tmp/sdd-val6/task-1-report.md).
Plan 6 T2: complete (Layer B corpus + the score probe + override channel + C8 lock. compat.json 17→27: 6 score probes — the B2 zero-override anchor compat-harish-manvitha [70/Strong, all 11 WEIGHT keys byte-exact: daystem_combo 0, daybranch −6, nayin −3, yongshin 9, ilju_pair 5, combined_elements 9, tengod_cross 10, daeun_sync 1, compat_stars 0, yin_yang 2, year_branch 2], the reversal compat-manvitha-harish-reversed [68/Strong, sole diff nayin −5], compat-mahesh-vp [64/Mixed = the _MA_VP_SCORES carrier], override-b-earth [74/Strong, yongshin 12 / ilju_pair 6 / combined_elements 11 — proves the override moves three sub-systems], + 4 charts="birth" subsystem probes [yongshin 9 / ilju_pair 5 / combined_elements 9 / daeun_sync 1]; 2 documented_interpretation rows: compat-harish-vinothini-pillars [planned 49, measured 43/Challenging — the plan's 49 was a full/PO figure applied to PO/PO; vinothini's birth data unrecoverable, documented not silently corrected] and compat-pawan-sruthi-stale [published 55, engine 60/Strong]. All 24 a_/b_ keys explicit per side. Edit A — the only src/ change, in the harness not the engine: check_compat_score (validation.py:472-491) now threads favorable_element_a/b into compat.compat_score instead of dropping them [product-visible: override b=Earth → 74, a=Fire → 64 band-stable/a-direction band-flip]. 3(c) plan-vs-plan contradiction resolved by _pillars_only_subsystem_entries() scoping T1's 7-element assertion by charts="pillars" while T2 covers all 11 WEIGHT keys across two chart sources. Step 1+6 module tests proven non-vacuous two ways [value: perturb tengod_cross 10→9 or composite 70→71 FAILs; shape: channel complement 7+4 with zero overlap]. Step 7 C8 not-symmetric lock + 2 guards; kind verified under --runxfail — genuine AssertionError "forward 64 vs reverse 66", not an ERROR [strict=False would accept the wrong reason]. Step 8 measured 3 passed/1 xfailed vs plan's 2 — the 4th -k match is the pre-existing false-friend test_compat_daybranch_asymmetric_scoring (L352, name/docstring assert reverse-direction detection, body asserts only a bounds check; logged, not fixed; does not collide with C8). Step 9 full-suite gate measured 836 passed/16 xfailed/0 failed [reconciles 824+8+2+2 / 13+2+1: the 2 documented_interpretation pytest.xfail() escapes are a THIRD xfail source, marker-only counts always undercount — plan predicted 11, others 14/15]. CLI 162 checks (PASS 157, INTERPRETATION 5, FAIL 0) exit 0, stdout md5 7047a056ad7879e80fb94d925f8adde7 byte-identical across two runs [152+10; 149+8 PASS; 3+2 INTERP = the two documented_interpretation rows, guard-not-verdict semantics hold], report md5 9854d2e1e5fdd3e1a743f754b8a1c89a. NEW FINDING: compat_stars is only VACUOUSLY pillars-only reproducible — compat.py:1153 reads a.stars or {}, empty for pillars-only charts, so the collapse is a FIVE-row table (published records −2), not A6's predicted four rows. Logged, not fixed. C8 lock + guards labelled DEFECT PINS in a comment block [unlike T1's fix-immune 육합 guards: on a sanctioned C8 fix the lock XPASSes AND both guards FAIL simultaneously, so the paired-update signal is unmistakable]. Fixture file byte-reproducible via json.dumps(..., indent=2, ensure_ascii=False) (13303 bytes). Report /tmp/sdd-val6/task-2-report.md).
Plan 6 T3: complete (W6 career subsystem — steps 1–5 were already on disk at resume [validation.py check_career L605 / check_career_tiers L629 / run_career_fixture L658 + registry wiring for `career` across _SUBSYSTEM_ORDER/_WORKSTREAM(W6)/_RUNNERS/_CHECKERS, all 2026-09-14 10:43; tests/validation/test_val_career.py 10:46; tests/validation/fixtures/career.json 10:48], steps 6–9 executed now). Step 6 — created tests/test_report_data.py (the plan says "modify (or create if absent)"; it was absent): the report_data stale-field bug-lock xfail(strict=False) + 2 plan guards + 1 added premise guard. THE DEFECT (spec §Known Open Bug 1, SECOND live instance — premium_report.py was the first): report_data._career_why L440 and _career_tiers L458/459 read the RAW strength_assessment["candidate_favorable"] instead of the element yongsin.favorable_element() actually resolves. Measured on harish (no override): raw candidate_favorable `Fire` / resolved `Water` method climate-balanced / _domain_element("Strategy & Consulting") `Water` / _career_why emits the *supportive-role* branch "Uses Water energy in a supportive role to your Metal Day Master." where the resolved element takes the *aligned* branch "Aligns with your favorable element Water, reducing friction." — the client-facing career prose contradicts the engine's own resolved 용신 on every row. Added premise guard test_stale_branch_is_reachable_for_a_domain_the_resolver_maps_to_water proves the "Water" in guard 1 comes from _domain_element, not a resolved match, so the guard cannot pass for the right reason; its name deliberately does NOT match the Step 7 -k expression (verified: 1 deselected). Step 7 measured `2 passed, 1 deselected, 1 xfailed in 0.60s` — matches the plan's "1 xfailed, 2 passed" exactly. Lock kind verified non-vacuous under --runxfail: genuine AssertionError on the INTENDED line (`assert 'favorable element Water' in 'Uses Water energy in a supportive role to your Metal Day Master.'`) AFTER both premise asserts pass, and all 3 guards pass independently (1 failed, 3 passed) — so it is a real defect pin, not a broken-setup xfail. Step 8 measured 878 passed / 17 xfailed / 0 failed [delta from the pre-Step-6 875/16 = +3 passed (2 plan guards + 1 premise guard), +1 xfailed (the lock) — exact]. Plan Step 8's prediction "5 + 5 + 1 + 1" = 12 is WRONG and so is Task 4 Step 7's restatement of 12; reconciliation 12 + 3 (the T1 육합 locks the plan's arithmetic omits) + 2 (the T2 compat documented_interpretation pytest.xfail() escapes) = 17 — 5th instance of the campaign's authoring-time miscount class, same root cause (marker-only counting omits the documented_interpretation escape class). CLI 194 checks (PASS 189, INTERPRETATION 5, FAIL 0) exit 0, stdout md5 9626959ad9b18f658fa49adb1b3ebeee byte-identical across THREE runs; report md5 abe8774b6cebc0f15571f197eeeff182; arithmetic 162+32 = 194 ✓ / 157+32 = 189 ✓; all 32 career rows render PASS because the 5 documented_interpretation rows are GUARDS not VERDICTS so INTERPRETATION stays at 5 [the 2 compat + 3 yongsin divergences]; scorecard `| career | 32 | 32 | 0 | 0 |` L16, `Current validation checks: 189/194 PASS across 8 subsystems` L355. TWO SPEC REFUTATIONS from measurement, not recall (/tmp/sdd-val6/probe-3c.out.txt): (a) B4 records sruthi's raw candidate_favorable as Wood and rejects a default-path sruthi assertion because "raw Wood == resolved Wood" — measurement gives raw **Metal** (so the default path ALSO diverges, Metal vs Fire, and B4's stated reason does not hold) and B4/A9's sruthi resolved values are **Fire** on the default path / **Earth** only with the chart-level override, not the Wood/Fire A9 records; (b) the chart-level override is NOT a compute_chart() kwarg (TypeError, verified) — it must be applied the way compat.py:1344-1355 does it (copy(a) + deepcopy(a.strength_assessment) + ["reader_override_favorable"] = element), which the plan's phrasings could be read as implying otherwise. Per the plan the sruthi instance is documented in the lock's comment and NOT locked (needs an override-applied chart; one chart pins the behaviour). _career_tiers shares the stale read and is pinned at FIXTURE level (career.json probe `tiers`, ordered 6-row tier list for harish + manvitha) rather than locked twice — strictly stronger than one field assertion; possible_tier_reachable False on both is structural (pools are 5×6 so i<7 is unreachable), pinned by test_pool_structure_invariant. Engine-untouched scope proof: every read-only engine mtime predates 2026-09-14 (report_data.py 2026-09-08, yongsin.py 2026-09-13 14:48, compat.py 14:50, premium_report.py 14:55), validation.py 10:43 is the ONLY src/ file carrying today, and grep -c candidate_favorable = 2 in BOTH report_data.py (the 2 pinned call sites) and premium_report.py (both pre-existing, pinned by Plan 5 T4). Fix landing signal: unlike the C8 lock whose guards fail simultaneously with it, a sanctioned report_data fix (two lines → favorable_element(chart).element) XPASSes the T3 lock while all 3 guards keep passing — the XPASS alone is the paired-update signal. Divergence re-confirmed: the plan's "flip the Plan 6 checkbox" names a convention this repo does not use; prose line appended instead, per the T1 precedent. Also re-confirmed: Step 5's "~14 entries" header vs 32 actual (authoring-time miscount class). Report /tmp/sdd-val6/task-3-report.md).
Plan 6 T4: complete (W6 CERTIFICATION + external cross-check, inline controller execution; the campaign's LAST workstream closure — the gate line it flips has no successor). Steps 1-2 — docs/research/2026-09-validation-compat-career.md (685 lines, md5 e5edf25e63b32ecd13021b5faba2e6fb, 65 http refs, 9 domains, 20 UNCERTAIN, 22 KB11 / 4 KB12 refs) is the survey every citation is drawn from, so the provenance chain is fixture -> research doc -> external source; /tmp/sdd-val6/apply-crossrefs.py stamps source["external"]={citation,url} plus the inline "(external cross-check 2026-09-14: ...)" suffix onto ALL 59 fixtures (27 compat + 32 career, malformed=[] = every object exactly those two keys), coverage asserted against the fixtures themselves (unmapped/stale id sets) so a later fixture without a mapping exits rather than passing unnoticed, and the script REFUSES to double-apply (sys.exit if source["external"] already present) so idempotence is by refusal not by silent overwrite. The external object is provenance metadata, NOT an expectation — it asserts no engine value, so no expectation was touched. Defect in the script's first draft: the 5 KB12 mis-map families were HAND-TYPED and 3 of the 5 were wrong (the ENGINE's value copied into the family slot; the 2 that came out right were right only because their engine value is None — wrong by accident, not by construction) — replaced with an AST read of KNOWN_MIS_MAPS out of tests/validation/test_val_career.py so neither slot can drift from what the suite asserts; the CLASS of error removed, not the instance. NEW FINDING: KNOWN_MIS_MAPS slot 0 is `_`-bound in the career test, so the KB12 family the fixture prose names is a value NOTHING pins — logged, not fixed (T4 Step 2 is metadata-only; editing T3's closed test file re-opens a closed task). Step 3 — _GATE_HISTORY W6 row checks 59 / passed 59 / gate_date 2026-09-14; the note was WRONG ON FIRST WRITE ("...+ 10 score/override anchors") and caught before the prose depended on it: measured by probe kind gives band 10 / score 6 / subsystem 11 = 27, so the figure was both wrong AND internally inconsistent (11+10+10 = 31 != the 27 in the same dict entry). Root cause = counting the SCORE helper dict's 10 keys (four of which — -yongshin, -ilju-pair, -combined-elements, -daeun-sync — are SUBSYSTEM probes) instead of the measured fixture population: the 6TH instance of the campaign's authoring-time miscount class. All six gate rows verified W1->W6 at 17/19/56/13/30/59 passed-equals-checks. Step 4 — the W6 prose block in render_report() is keyed to `career` because W6 is the only workstream spanning TWO subsystems and `career` is last in _SUBSYSTEM_ORDER, so it renders EXACTLY ONCE and AFTER BOTH W6 detail tables, the way the W4/W5 blocks follow theirs; the reason is in a code comment so the next editor does not "fix" the key to `compat`. Content (every figure measured, none transcribed): the 27-row split; the 112-vs-100 weight reconciliation with WEIGHT['combined_elements']=12 as the gap (a 12th weighted key or a 113 total silently breaks every published number); the band cut-points >=80/>=65/>=45/else with all 10 pinned edges; the ZERO-OVERRIDE anchor compat-harish-manvitha reproducing 70/Strong exactly from FULL/FULL birth pipelines asserting all 11 WEIGHT keys, plus the override anchor moving to 74/Strong (a reader override perturbs yongshin AND ilju_pair AND combined_elements); the cross-branch priority 육합->육충->육해->육파->형->반합 at compat.py:310 with reachable 형 子卯/丑戌, the 5 shadowed pairs (寅巳, 巳申, 寅申, 丑未, 戌未) and the four self-punishments 辰午酉亥 at -2; career 30 domain-map + 2 tiers with 25 agreeing and 5 guarded mis-maps; the tier cut-points i<4/i<7/else with Possible STRUCTURALLY UNREACHABLE (pool 5x6=30 but only 6 candidates); the 2 documented divergences (harish x vinothini = a chart-INPUT difference since the published 57/100 is a FULL/FULL measurement vs the fixture's pillars-only reconstruction; pawan x sruthi = STALE CHART predating the 야자시 fix); the 4 pinned bug-lock families / 10 markers / 10 guards distinguishing fix-immune guards (육합 3/3, C9 5 locks/2 guards) from deliberate DEFECT PINS (C8 1/2, the report_data lock 1/3 — the 2nd live instance of the premium_report bug class), including the C9+육합 interaction (巳申 is simultaneously 六合 and 六破 and 육합 is tested first, so a C9 fix alone flips 5/5 while C9+육합 flips only 4/5); and the external cross-check line. Step 5 — gate line flipped to "Campaign COMPLETE — all six workstreams (W1-W6) certified; no successor gate." Step 8 REVERSIBILITY (no VCS diff exists in this tree): proved the Step-2 edit FAITHFUL by writing its INVERSE (strip source["external"] + regex-strip the inline suffix), re-applying the forward transform, and requiring byte equality — compat forward(inverse)==current YES, career YES, and every NON-source field identical across all 27/32 entries YES, so the byte delta is fully attributable to the transform: compat 50594 -> 31674 B (delta 19333 = inline 8115 + objects 11218), career 40424 -> 18432 B (delta 22084 = inline 8080). FINDING: T2's recorded "13303 bytes" for compat.json does NOT reproduce — pre-Step-2 measured 31674 B and NO serialization tried yields 13303 (nearest candidate, the 17-entry prefix, 13622 B, is off by 319), so the figure is neither the 27- nor the 17-entry file; the CLAIM (byte-reproducibility by json.dumps(indent=2, ensure_ascii=False)+"\n") is independently confirmed here by the round trip, the FIGURE is not — recorded as a discrepancy, not silently reconciled. Also caught mid-sweep: json.dumps returns a str so len() counts CHARACTERS while file size counts UTF-8 BYTES (compat recovered 31261 chars vs 31674 bytes — Korean is 3 B/char), so every size claim must state which. Gates: suite 878 passed / 17 xfailed / 0 failed (unchanged from T3 — T4 adds documentation, not checks); CLI 194 checks PASS 189 / INTERP 5 / FAIL 0 exit 0 x2 with stdout md5 9626959ad9b18f658fa49adb1b3ebeee byte-identical across runs AND identical to T3's recorded value — that is the point, stdout carries only the summary line so a prose/gate-line change CANNOT move it, which is what makes it a usable cross-task invariant while the REPORT md5 legitimately moved abe8774b6cebc0f15571f197eeeff182 -> 7135b025521e276cfec4e795be6bfa94; cmp -s proved stdout AND report byte-identical across the two runs => idempotent at the new md5. Report surfaces: `| compat | 27 | 25 | 2 | 0 |` L15, `| career | 32 | 32 | 0 | 0 |` L16, W6 prose heading L348, certification L377-382 (W6 CERTIFIED 2026-09-14 at 59/59 with its note in parentheses), `Current validation checks: 189/194 PASS across 8 subsystems` L383, gate line L384; arithmetic untouched (162+32=194, 157+32=189) because the prose block is documentation. Report body produced THROUGH render_report() ONLY — the validation report markdown was never hand-edited, so the epilogue stays derivable from code (idempotence proven by the byte-identical second run). Loose end (a) RESOLVED, not deferred: no `notes` field was written because no NEW discrepancy surfaced at T4 beyond the already-recorded mis-map root causes, and all 7 documented_interpretation rows were verified to already carry their divergence mechanism in notes (1713/773/262/193/301/222/233 chars, none empty). NEW FINDING (self-referential measurement): the inherited ledger checkbox check — the one T1-T3 recorded as returning 0 — now returns 1, because T1's own prose line QUOTES the literal pattern, so the grep counts its own quoted claim; the ANCHORED form returns 0 and the only bracket-x token in the file is that quotation, so the convention claim holds and the 1 is an instrument-polluting-itself artifact. Deliberately NOT re-spelled here (that would plant a second occurrence); exact commands live in the T4 report, and the anchored form is the check to use. Engine-untouched scope proof: every read-only engine mtime predates 2026-09-14 (report_data.py 2026-09-08 10:43, nayin.py 2026-07-06 11:33, yongsin.py 2026-09-13 14:48, compat.py 14:50, premium_report.py 14:55, climate.py 14:25) and validation.py 11:17 is the ONLY src/ file carrying today. W6 CERTIFIED — all six campaign workstreams certified, gate names no successor. Next: Plan 6 T5 (final whole-plan review -> /tmp/sdd-val6/plan-6-final-review.md), then campaign close. Report /tmp/sdd-val6/task-4-report.md).
Plan 6 T5: complete (final whole-plan review -> /tmp/sdd-val6/plan-6-final-review.md; verdict READY, 0 Critical / 0 Important. All five steps discharged with INDEPENDENTLY RE-RUN figures, not transcribed from T1-T4). Step 1 (gates) — suite 878 passed / 17 xfailed / 0 failed in 26.83s [T4 recorded 878/17 in 25.77s: COUNTS identical, wall-clock differs, which the gate does not assert]; CLI exit 0 x2, stdout md5 9626959ad9b18f658fa49adb1b3ebeee IDENTICAL to T3's and T4's recorded value, report md5 7135b025521e276cfec4e795be6bfa94 IDENTICAL to T4's, stdout byte-identical across runs, stderr 0 B => idempotent and the report body stays derivable from render_report() (a hand-edit would have moved the report md5 and the two runs would disagree); checks: 194 (PASS 189, INTERPRETATION 5, FAIL 0). CROSS-SURFACE CONSISTENCY established (new): the _GATE_HISTORY W6 row's passed=59 is the scorecard's PASS PLUS INTERPRETATION columns (25+2=27, 32+0=32) — a documented_interpretation divergence is a guard-PASS not a verdict-fail, so it counts in passed; the two surfaces use different words for the same fact. Campaign delta vs the plan baseline (plan L263): 791->878 passed (+87), 5->17 xfailed (+12, all W6), 135->194 checks (+59 = the W6 fixture count exactly), stdout md5 b2ca53714fcaf20107c63ac42465cf76 -> 9626959ad9b18f658fa49adb1b3ebeee. Step 2 (sourcing) — 59/59 W6 fixtures carry source with citation + url_or_ref + type + external, 0 malformed, and all 7 documented_interpretation rows state their divergence mechanism in notes (1713/773/262/193/301/222/233 chars, none empty); the external object is provenance metadata NOT an expectation, so the measured values remain ground truth. Step 3 (lock non-vacuity) CLOSED BY MEASUREMENT rather than reasoning alone, on BOTH required surfaces: (a) guards still pass, run as an explicit 10-guard named roster -> 15 passed in 0.55s (15 instances from 10 test functions because test_priority_ladder_reports_each_relation_kind is parametrized), so 'guards pass' is demonstrated per-test and not inferred; (b) removing the behaviour (simulated in-process via monkeypatch, engine untouched) breaks a guard for BOTH defect-pin families — C8: making compat_nayin order-independent XPASSes the lock, FAILS Guard 2 on both assertions [(fwd.score,rev.score) 70,68 -> measured 68,68; differing ['nayin'] -> measured []] and FAILS Guard 1 ASYMMETRICALLY (forward half band=='Mixed' / nayin.score==3 STILL HOLDS; only the reverse half band=='Strong' / nayin.score==5 fails, rev collapsing 66/Strong/nayin5 -> 64/Mixed/nayin3) => the fix collapses BOTH directions onto the FORWARD value, not a midpoint, and the block comment's 'BOTH guards FAIL' is true at TEST granularity but misleading at ASSERTION granularity because Guard 1's forward half never moves and so cannot signal the fix; report_data: a fixed_why reading favorable_element(chart).element moves the emitted string 'Uses Water energy in a supportive role to your Metal Day Master.' -> 'Aligns with your favorable element Water, reducing friction.', XPASSes the lock, FAILS ONLY Guard 1 (the byte-exact defect pin) while Guard 2 ('Water','climate-balanced') AND the premise guard both survive — exactly the predicted shape, a signal rather than a wall of red. All three families verified non-vacuous: each has >=1 assertion that genuinely moves when the behaviour is removed and >=1 witness that does not. Step 4 (engine untouched) — compat.py 2026-09-13 14:50:26, report_data.py 2026-09-08 10:43:38, nayin.py 2026-07-06 11:33:07, yongsin.py 2026-09-13 14:48:11 all predate the W6 window, validation.py 2026-09-14 11:17:04, and `find src -name '*.py' -newermt '2026-09-14 00:00:00'` returns EXACTLY ONE file: src/saju_engine/validation.py — every W6 defect is PINNED NOT FIXED, which is what makes the fixtures validations rather than assertions of the engine's opinion. Step 5 (spec coverage row) mapped by reading the fixture files rather than re-asserting — F1 the 11 compat sub-systems: compat.json n=27 split band 10 / subsystem 11 / score 6, the 11 subsystem ids enumerated (c-pillars-daystem-combo, c-pillars-daybranch, c-pillars-nayin, c-pillars-tengod-cross, c-pillars-compat-stars, c-pillars-yin-yang, c-pillars-year-branch, compat-harish-manvitha-yongshin, compat-harish-manvitha-ilju-pair, compat-harish-manvitha-combined-elements, compat-harish-manvitha-daeun-sync) = 11/11 one named fixture each, the 10 band rows pinning every _band_for cut-point edge (-1/0, 44/45, 64/65, 79/80, 100/101) and the 6 score rows pinning the composite plus the 112-vs-100 reconciliation and both anchors, with chart binding measured {None:10, 'pillars':8, 'birth':9} (so the chart-bound split is 8 pillars / 9 birth: the 11 subsystem probes are 7 pillars + 4 birth, and the 6 score rows add 1 more pillars-only reconstruction + 5 birth rows); F2 favorable-spouse signals through day-branch relations: c-pillars-daybranch expected {'score': -6} VERIFIED True and c-pillars-year-branch expected {'score': 2} VERIFIED True, plus test_compat_cross_branch_score_covers_punishment_and_break (육파 子酉 -1, 삼형 丑戌 -2, 반합 寅午 +2, 자형 辰辰 -2, neutral 子/午), the C9 5-marker lock and the C8 1-marker lock, so the priority ladder 육합->육충->육해->육파->형->반합 (compat.py:310) is pinned in order; F3 career-domain tiers: career.json n=32 split domain_map 30 / tiers 2, 25 of 30 agreeing with KB12 and 5 guarded mis-maps (2 coverage gaps returning None, 3 substring/ordering artifacts), both tiers rows (career-tiers-harish, career-tiers-manvitha) measuring possible_tier_reachable FALSE with 6 tiers — mechanism read from test_val_career.py:110-115: the pool is 5x6=30 but there are only 6 candidates, so the i<4 / i<7 / else ladder tops out at index 5 and Possible is STRUCTURALLY unreachable, asserted at test_val_career.py:175, which also pins the KB12:130-152 Day-Master-keyed contradiction. GAPS LISTED rather than smoothed over: (1) the Possible tier is covered only as a NEGATIVE — no fixture can exercise a chart that reaches it, so if the pool ever widens nothing would notice; (2) 六合 as a SCORING signal is uncovered by necessity (the 3 locks + 3 named guards exist, but the engine path is dead code, so the relation is covered and the effect is not — correctly, since there is no effect to measure); (3) 형 coverage is partial by construction (C9 shadows 5 of 7 pairs, only 子卯/丑戌 reachable); (4) KNOWN_MIS_MAPS slot 0 unasserted; (5) the 11 sub-systems run through ONE probe kind, with the per-subsystem weights asserted collectively by test_weight_reconciliation rather than one fixture per WEIGHT key. No gap in the NOMINAL coverage: every family the spec names maps to at least one named fixture; gaps 1-3 are engine properties the fixtures faithfully record. LOCK INVENTORY: 4 families / 10 non-strict xfail markers / 10 guard test functions -> 15 guard instances, all passing — C9 삼형 shadowing (5 markers, 2 guards = 6 instances, FIX-IMMUNE: the guards use inputs a sanctioned fix cannot touch, so they must keep passing = the 'the fix did not break the rest of the ladder' witness), C8 order-dependence (1/2, DEFECT PIN), report_data stale field (1/3 = 2 witnesses + 1 premise, DEFECT PIN), 육합 dead code (3/3, FIX-IMMUNE), plus the 2 documented_interpretation escapes which are dynamic pytest.xfail() calls and carry no bug claim. DEFECT PIN = the guard asserts the measured WRONG value, so the fix makes the lock XPASS AND the guard FAIL — the paired-update signal that the pin must be deleted with the fix. FINDINGS, all MINOR/INFORMATIONAL, none Critical, none Important: M1 the plan's own xfail arithmetic is self-consistent but describes a DIFFERENT 12 — plan L759 expects '5 (W4) + 5 (C9) + 1 (C8) + 1 (stale field) = 12' while 17 occur; the plan's four terms decompose as 5 NON-W6 (W4's 3 + W5's 2, i.e. the '5 (W4)' label actually BUNDLES two workstreams) + 7 W6, whereas W6's real 12 = 5 C9 + 1 C8 + 1 stale + 3 육합 + 2 documented_interpretation — two groupings that BOTH total 12 while sharing only 7 members, which is exactly why the miscount survived authoring review (the arithmetic checks out; the referents were substituted); the gate was never wrong, only the expected figure — the 7TH instance of the campaign's authoring-time miscount class, not fixed because the plan is a historical artifact. M2 the marker-grep census picks the WRONG UNIT: `grep -rn '@pytest.mark.xfail' tests/` returns 8 sites where pytest reports 17 instances, reconciling as 12 DECORATED (one site parametrized x5 = C9, plus C8, stale, W5 x2, 육합 x3) + 5 DYNAMIC pytest.xfail() runtime calls no decorator grep can see (W4's 3 at test_val_yongsin.py:32, W6's 2 documented_interpretation rows at test_val_compat.py:91); a future census must count instances via -rxX, not sites. M3 KNOWN_MIS_MAPS slot 0 is `_`-bound so the KB12 family the fixture cross-check prose names is a value nothing in the suite pins (not fixed: editing Task 3's closed test file is outside scope). M4 Task 2's 13303-byte compat.json figure does NOT reproduce — pre-Step-2 measured 31674 B / 31261 chars, no serialization tried yields 13303, nearest candidate (the 17-entry prefix, 13622 B) is off by 319; the CLAIM is independently confirmed by T4's round trip, the FIGURE is not, and no gate rests on it. M5 Task 5 Step 3's wording says '3 new xfail locks' where there are 4 families (육합 omitted) — under-count in the plan's prose only; all four were verified non-vacuous. M6 the ledger-checkbox measurement artifact — the inherited grep -c '- [x]' now returns 1 not 0 because T1's own prose line QUOTES the literal pattern, so the instrument's description pollutes the instrument; the ANCHORED form `grep -c '^- \[[x ]\]'` returns 0 and the prose-line convention holds; use the anchored form. I1 _CHECKERS is confirmed DEAD (collect_results reads _RUNNERS only) — prune candidate, not pruned in a review-only task. I2 the hand-typed-dict defect class (3 of 5 KB12 families wrong, the 2 right ones right only because their engine value is None) — the CLASS was removed by AST derivation, not the instance. I3 the _GATE_HISTORY W6 note's own correction (counting a helper dict's keys instead of the measured fixture population; 11+10+10=31 != the 27 in the same entry — 6th instance of the miscount class). I4 C8 Guard 1's asymmetric failure, stated for the future editor. SPECIFICATION GAP (not a defect) — the C9+육합 interaction 'a C9 fix alone flips 5/5 while C9+육합 flips only 4/5' holds ONLY for a fix shape that PRESERVES 육합's priority at compat.py:310 (삼형 slotted after 육합); a fix that hoists 삼형 above everything unconditionally flips 5/5 and the comment's warning never fires, because 巳申 is simultaneously 六合 and 六破 and 육합 is tested first — the warning is correct for the more likely fix and misleading for the other, so the claim's SCOPE is what needs recording; no change made (T5 is review-only and editing W6's prose would move the report md5 and re-open a closed task). T5 edited NO repository file (review-only) and the validation report markdown was NEVER hand-edited, proven by the byte-identical second CLI run).
Plan 6 (W6 compat / spouse + career) FULLY CLOSED — W6 CERTIFIED 2026-09-14. CAMPAIGN COMPLETE: all six workstreams certified — W1 pillars 17, W2 대운/세운 19, W3 십신/12운성/납음/공망 56, W4 용신 13, W5 climate 30, W6 compat + career 59 = 194 checks (PASS 189, INTERPRETATION 5, FAIL 0, 8 subsystems), 0 failed. Suite 878 passed / 17 xfailed / 0 failed. CLI exit 0, stdout md5 9626959ad9b18f658fa49adb1b3ebeee, report md5 7135b025521e276cfec4e795be6bfa94, idempotent across runs (stdout AND report byte-identical), report body produced through render_report() only. Gate line reads 'Campaign COMPLETE — all six workstreams (W1-W6) certified; no successor gate.' Final review READY: /tmp/sdd-val6/plan-6-final-review.md — 0 Critical / 0 Important, 6 Minor + 4 Informational documented, every gate figure independently re-run by the controller rather than taken from a task report. Lock inventory at close: 4 families / 10 non-strict xfail markers / 10 guard tests -> 15 guard instances all passing, classified fix-immune (C9, 육합) vs defect-pin (C8, report_data). Engine untouched across the whole campaign's W6 window (validation.py the only src/ change). Defects PINNED NOT FIXED and carried forward, client-visible ones first: (1) premium_report.py _right_now_callout / ctx_favorable_phrase raw-favorable read — LIVE-WRONG in the Pawan/Sruthi client reports; (2) report_data._career_why / _career_tiers stale-favorable read — the SECOND live instance of the same bug class, contradicting the engine's own resolved 용신 on every career row; (3) 육합 dead code (SIX_COMBINATIONS is a 3-tuple table while _branch_pair_lookup tests 2-tuple membership, unreachable at compat.py:310 [+4], :407 [primary spouse-palace +20, where the engine's own narrative calls it 'the strongest favorable indicator in classical 궁합' yet reports the pair as having 'no direct 합/충' — a false statement in a paid deliverable], :1295 [띠 +3], :428 [vacuous 반합 suppression, latent only]); (4) C9 삼형 shadowing (5 of 7 형 pairs shadowed by higher-priority 합충파해); (5) C8 order-dependence (compat_score not symmetric, nayin direction-dependent, 70 vs 68); (6) KB12:130-152 Day-Master-keyed tier-pool contradiction; (7) regenerate the Pawan/Mahesh client reports for the 야자시 fix and refresh Harish's stale career / land-workshop / luck-timeline files; (8) md_to_saju_compat_pdf.py --output path bug; (9) Harish longitude discrepancy (76.33 in sewoon.json vs 79.42 elsewhere — test_report_data.py uses 79.42); (10) the 조후-vs-억부 gate-width product decision; (11) the 辰/戌 divergence with the 丑/未 convergent-validity explanation; (12) knowledge/17-climate-method.md:51-52 wording defect; (13) prune the dead _CHECKERS registry. Campaign has no successor gate — the six workstreams are closed.
Carried defect #6 (post-campaign fix, 2026-09-14): complete — engine fixed, every lockstep surface moved with it, controller-verified from re-run figures. THE DEFECT (two halves, one root): report_data._career_why L440 and _career_tiers L458/459 read the RAW strength_assessment["candidate_favorable"] instead of the element yongsin.favorable_element() actually resolves (spec §Known Open Bug 1, SECOND live instance — premium_report.py was the first), AND the tier candidate pool was keyed on the Day Master's own element family, contradicting KB12:130-152 (align work with the chart's 용신, not the DM's own element). FIX — src/ change confined to report_data.py: new _resolved_favorable(chart) helper is the single read point (returns "" when the resolver cannot name an element, so callers compare falsy rather than matching the "—" sentinel against a real element name); both call sites read it; _career_tiers now builds family_order = [e for e in (favorable, supporting) if e in _CAREER_DOMAINS] with the DM family only as a never-empty fallback — the pool is NEED-KEYED, 12 candidates from 2 families on both fixtures, so the i<7 **Possible** band is live. _CAREER_DOMAINS itself UNCHANGED: KB12:25-49 names it as the reference lookup, and the 5x6 invariant + test_knowledge_grounding + test_domain_map_covers_exactly_all_30_kb12_domains all pin its 30 domains. DELIBERATE ASYMMETRY, recorded in both the helper's module docstring region and the test file: the -2 penalty branch stays on the RAW candidate_unfavorable, because FavorableElement (yongsin.py:75-102) publishes NO display-channel unfavorable element at all — it carries only `element` and `supporting`. There is no resolver to read, and inventing one would assert a rule the knowledge files do not state. Latent today: harish's raw unfavorable is None, manvitha's is "Wood", and no Wood-family domain reaches her pool, so the -2 branch fires on neither tier fixture. LOCKSTEP SURFACES (all four moved together — leaving any one behind would have kept its pin passing vacuously): (1) tests/validation/fixtures/career.json — both tiers rows re-pinned to 12 rows with possible_tier_reachable: true; (2) tests/validation/test_val_career.py — test_tiers_entries_pin_possible_reachable_on_both_charts is now a POSITIVE pin (asserts the flag is literally True, len(tiers)==12, and >=1 **Possible** row, because a truthy flag alone would survive a pool that silently narrowed back to one family) and test_pool_structure_invariant's docstring now separates the 5x6 reference-table invariant from the pool size; (3) tests/test_report_data.py — the xfail(strict=False) lock XPASSed on the fix, which is the delete-the-marker signal, so the marker is RETIRED and the file now pins the fixed behaviour positively: test_career_why_uses_resolved_favorable_element + byte-exact Guard 1 (asserts the served string "Aligns with your favorable element Water, reducing friction.") + Guard 2 (harish resolves to ("Water","climate-balanced")) + a premise guard pinning that the RAW field is still "Fire" while _domain_element("Strategy & Consulting") == "Water" — without that divergence pin Guard 1 would keep passing while silently ceasing to discriminate resolved from raw; (4) src/saju_engine/validation.py — 4 render-prose edits (career paragraph now "32 rows: 30 domain-map + 2 need-keyed tier charts"; the Tier cut-points paragraph rewritten to state the resolved-용신/희신 pool and to record that it CLOSES the KB12:130-152 contradiction; the count line now "3 pinned bug-lock families / 9 non-strict xfail markers / 10 guard tests" with the FIXED-2026-09-14 note; the stale report_data bullet DELETED so the list renders as exactly three bullets) + the _GATE_HISTORY W6 row note rewritten to match. MARKER ARITHMETIC reconciled against the suite's own count rather than recalled: the retired item was ONE pytest marker — and one @pytest.mark.xfail on a PARAMETRIZED test yields N xfail instances, which is why C9's single decorator sits on a 5-param list and contributes 5 — so W6 xfail instances go 10 -> 9 (5 C9 + 3 육합 + 1 C8 + 1 report_data) and the campaign 17 -> 16, which is exactly what pytest reports. W6 GUARD tests stay 10: the retired item was a lock, not a guard, so nothing in the guard taxonomy moved. SECOND-ORDER CONSEQUENCE, SURFACED not silently absorbed: this fix necessarily resolved BOTH halves of carried item (2) of the two-channel element-resolution bug class (report_data.py:440 AND :458), because the ranking and the per-row prose had to move to the display channel together to stay consistent — a ranking on the resolved element with prose on the raw one would have been a new defect. Recorded in the render prose and in _GATE_HISTORY. premium_report.py:181 / :1395 (items 1-3, authorised but not selected) remain the only unfixed half of that class. W6 COVERAGE GAP 1 CLOSED (the **Possible** tier was code-reachable but output-unreachable, so no fixture could exercise it) and recorded as closed in both surfaces. CONTROLLER VERIFICATION — every figure below re-run by the controller at entry-writing time, not transcribed from a task report: suite 879 passed / 16 xfailed / 0 failed in 27.21s; CLI exit 0 on two consecutive runs, `checks: 194 (PASS 189, INTERPRETATION 5, FAIL 0)`, stderr 0 B both runs; stdout md5 9626959ad9b18f658fa49adb1b3ebeee IDENTICAL to the campaign-close value — the stdout md5 is the cross-task invariant precisely because the CLI's stdout carries only the summary line, so it cannot move while the counts hold, and it did not; report md5 moved 7135b025521e276cfec4e795be6bfa94 -> ac526882415801fef0628f769f89a600, which is LEGITIMATE and expected (the body is regenerated through render_report() and the career prose genuinely changed) — and because a hand-edit would have been indistinguishable from this by md5 alone, the proof that the body still comes from code is the idempotence pair: two runs byte-identical in stdout AND report, with the report md5 stable across them. Campaign-close baseline 878 passed / 17 xfailed -> 879 / 16 is exactly +1 passed and -1 marker, i.e. the retired report_data lock and its companion marker — no other test changed state. An intermediate 3-failed run observed mid-edit (875 passed / 16 xfailed / 1 xpassed) was the transient lockstep state — engine fixed, stale pins not yet moved — and is not the baseline; the lockstep updates are what took it to green, which is the whole reason the four surfaces must land together. Remaining bookkeeping for this item: the two residual stale surfaces left by the fix (the tests/validation/test_val_career.py module docstring sentence still describing "the engine's *current* Day-Master keyed pool selection, which contradicts the KB12:130-152 classical priority", and the MEMORY.md campaign line still reading "878 passed / 17 xfailed") are being corrected in the same pass.
Carried defect #11 (辰/戌 Temperate-vs-Cold/Hot band divergence, climate.py) — RESOLVED 2026-09-14 as a DOCUMENTATION + SCOPE fix, NOT a model change. Decision: option (B) of two. Option (A) — extend climate.py to fire the 燥濕 override (辰→Fire, 戌→Water) — was REJECTED on three grounds: (1) the engine already agrees with the fuller classical reading on all ten branches where it prescribes an element at all, so no element is computed wrongly anywhere in the subsystem; (2) the per-branch assignments rest on a modern practitioner source (cantian.ai, directly reviewed 2026-09-13), not a transcribed classical text — Ground Rules 1-2; (3) widening the model changes the client-facing `band` value for every 辰/戌-month chart, which is the same class of PRODUCT decision as #10, not a defect to be patched. FOUR surfaces moved, all documentation/citation — ZERO behaviour change: (1) knowledge/17-climate-method.md restructured: old '## The Three Climate Bands' + 'Simplification flagged [UNCERTAIN]' replaced by '## The Three Climate Bands — The Engine's Implemented Model' (same table, now labelled the 寒暖-only axis, explicitly 'a deliberate subset of the fuller classical treatment — not a claim that the fuller treatment does not exist') + '## The Fuller 寒暖燥濕 Reading — Sourced, Not Implemented' (적천수 couplet 天道有寒暖，发育万物 … 地道有燥湿; 4-row branch/remedy table 亥子寒→Fire, 巳午暖熱→Water, 戌未燥→Water, 辰丑濕→Fire; provenance paragraph naming cantian.ai directly reviewed, OpenFate 2026-07-22 framing-only, SajuGallery 庚金 line UNVERIFIED; three comparison bullets incl. convergent validity for 丑/未 — engine cuts by seasonal quartet, source by damp/dry state, both land Fire for 丑 / Water for 未; §Why the fuller reading is not implemented giving the two Ground-Rule reasons and naming the two pins; [UNCERTAIN] NARROWED to the per-branch assignments, not the principle). (2) src/saju_engine/climate.py — scope paragraph inserted into the module docstring at L11-19 naming the 寒暖-only limit, the 辰/戌 divergence, and the pins. Docstring-only; module body byte-identical. (3) tests/validation/fixtures/climate.json — four `source` strings (band-chen L22, band-wei L46, band-xu L70, band-chou L94) now cite §The Fuller 寒暖燥濕 Reading with the sourced element plus the documented-scope / convergent-validity distinction. `expected` and status:'expect_match' UNTOUCHED, so no count moves. Pre-verified safe: the only test touching fixture `source` is tests/validation/test_loader.py:20, which asserts non-emptiness only. (4) src/saju_engine/validation.py render_report() 'Open coverage gaps' paragraph (was L1049-1059) — dropped the dead hard line-number citation knowledge/17-climate-method.md:49-54 (Edit 1 moved those lines and renamed the section) and the stale [UNCERTAIN] framing; replaced with the measured ten-branch agreement, the 'only in whether an override fires, never in which element' narrowing, the convergent-validity note, and the reframe as a pinned explicit SCOPE LIMIT whose expansion would move a client-facing field. Plus two test docstrings updated so the pin documents itself: tests/test_climate.py::test_temperate_branches_have_no_override and tests/validation/test_val_climate.py::test_temperate_rows_have_no_climate_element — both now name 辰/戌 as the pinned divergence and state that widening the engine to the four-way reading fails THERE (the unit half of the pin whose fixture half is band-chen / band-xu). VERIFICATION (controller-run, not executor-reported): pytest 879 passed / 16 xfailed / 0 failed — unchanged. CLI exit 0, 'checks: 194 (PASS 189, INTERPRETATION 5, FAIL 0)'. stdout md5 9626959ad9b18f658fa49adb1b3ebeee — IDENTICAL, so the cross-task invariant held. Report md5 ac526882415801fef0628f769f89a600 -> 251c795b8a7f54aa4decd84a38b1b159 (legitimate: rendered prose changed), and a re-run reproduces 251c795b8a7f54aa4decd84a38b1b159, so the report is still idempotent. No corpus candidate has 辰 or 戌 as its month branch, so nothing published changes. OUTSTANDING: the model EXPANSION itself is surfaced to the user as a product decision alongside #10 — not decided here.

Carried defect #5 (C8 order-dependence, 2026-09-14): complete — engine fixed, all four lockstep surfaces moved, both gates re-run, controller-verified from measured figures (never from prior output). THE DEFECT: compat_score(a, b) was NOT symmetric — the nayin sub-system is direction-bearing (the six relation labels name WHICH tone generates or overcomes which), and compat_nayin read that direction off the CALLER's argument order rather than off the couple, so one couple had two verdicts (Mahesh × VP forward 64/Mixed vs reverse 66/Strong; Harish × Manvitha 70/Strong vs 68/Strong, nayin −3 vs −5). THE FIX: compat.py::_canonical_nayin_pair(a, b, nayin_a, nayin_b) resolves the SUBJECT order before the relation is computed AND displayed — male-first when both genders are known and differ (matching the product's canonical partner order for a heterosexual pair), else the tone appearing earlier in NAYIN_ORDER. The index tiebreak is DECLARED a stability rule, not a recovered classical rule: knowledge/11-gunghap.md §C step 3 specifies an ordered 30×30 table, but no sourced text consulted defines an order rule at all (the table is still seeded by the documented 5-element grammar reduction, pending the published 서전구미록 table), so the engine declares its own canonical order rather than inventing a classical one — the same gender-declining move as the §G spouse-star mapping. Both branches are a NO-OP for every already-canonical (male-first) pair, which is the load-bearing safety property: no published verdict moves and no documented weight changes (the _score_map normalization 상합 +3→+5 / 상구 +2→+3 and the sub-system max are deliberately untouched). FOUR LOCKSTEP SURFACES: (1) src/saju_engine/compat.py — the helper, the _NAYIN_INDEX table, the canonicalized relation call, and the display switch so flag/narrative print the canonical pair; (2) knowledge/11-gunghap.md §C — new "### Canonical subject order (engine convention)" subsection between procedure step 5 and the data-gathering note; (3) tests/test_compat.py — the C8 lock RETIRED (its XPASS was the delete-the-marker signal — the 2nd consecutive application of that rule after #6) and its two defect pins re-pinned to order-independence, now 3 positive guards including an all-subsystems-identical assertion; (4) tests/validation/fixtures/compat.json — two entries re-pinned. MEASURED VERIFICATION (all controller-run): (a) symmetry sweep over 12 charts × 3 gender configurations — M/F cross-product 144 pairs, None/None 12, M/M 12 — with 0 composite-differing, 0 subsystem-differing and 0 differing-and-not-only-nayin in EVERY configuration, so the fix is total, not partial; (b) display canonicalization confirmed — both call orders print an identical flag (harish×manvitha → 납음 釵釧金 vs 山頭火 → 상해 [element-grammar-fallback], 70/Strong/−3; mahesh×vp → 납음 城頭土 vs 大海水 → 상충, 72/Strong/−5); (c) all 27 compat fixtures re-measured — exactly 2 non-PASS, the 2 PRE-EXISTING documented_interpretation divergences (harish×vinothini 57 vs 45; pawan×sruthi 55 vs 60), hence no regression; (d) NO-REGRESSION PROOF by monkeypatching _canonical_nayin_pair to identity, which restores exact pre-fix semantics — its own sanity check reproduced the OLD fixture text ("score: expected 70, got 68; subsystems.nayin: expected −3, got −5") — and under it c-pillars-nayin and compat-manvitha-harish-reversed both FAIL while both INTERPRETATION entries stay INTERPRETATION either way. FIXTURES MOVED (3, not the 2 predicted): compat-manvitha-harish-reversed 68/Strong/nayin −5 → 70/Strong/nayin −3; c-pillars-nayin −3 → −5 — the pillars-only path supplies NO gender, so the index tiebreak applies (chart A day 辛亥 = 釵釧金 idx 23; chart B day 甲戌 = 山頭火 idx 5; lower index is subject → (山頭火 Fire, 釵釧金 Metal) → OVERCOMES[Fire]=Metal → 상충 −5; pre-fix the caller's given order gave the reverse → 상해 −3). CORRECTION TO MY OWN PRIOR OUTPUT, carried deliberately: I had predicted c-pillars-nayin would be UNCHANGED, having the two charts' Nayin swapped and mis-indexing 山頭火 as 27 instead of 5 — only re-measurement caught it. That is the 2nd consecutive window in which a remembered assumption proved wrong and measurement was the only thing that caught it: the controller rule applied to my own output. HARNESS + A CORRECTED COUNT: validation.py's _GATE_HISTORY W6 note and the open-bugs render block were the last lockstep surface — the C8 bullet is DELETED and both count lines re-derived by INSPECTION, not arithmetic. The note had read "3 pinned bug-locks / 9 non-strict xfail markers / 10 guard tests"; re-measured, NO scoping reproduces 9 (W6-scoped is 6 markers, suite-wide is 8). Root cause of the drift: the tally mixed units — MARKERS of one figure, parametrized TESTS of another (1 decorator on the 5-way C9 parametrization = 5 xfailed tests) — and 5 of the 15 xfailed tests are not defect pins at all but legitimate documented_interpretation divergences. The figure is therefore CORRECTED IN PLACE rather than carried, and the correction is stated in the rendered report itself so the next reader sees both the old value and why it was wrong. Current measured state: 2 live fix-immune families (육합 3 locks / 3 guards; C9 1 parametrized lock over 5 pairs / 2 guards) = 4 W6 markers → 8 xfailed tests, 5 markers suite-wide once the 2 client-visible climate locks are counted, 8 W6 guards; the report_data (#6) and C8 (#5) families are retired. GATES (both re-run AFTER the harness edit): suite 880 passed / 15 xfailed / 0 failed (was 879/16/0 — the C8 block swap retires exactly 1 lock and adds exactly 1 pass, as predicted); CLI exit 0, "checks: 194 (PASS 189, INTERPRETATION 5, FAIL 0)", stdout md5 9626959ad9b18f658fa49adb1b3ebeee UNCHANGED from baseline (stdout carries only the summary line, so it is the cross-task invariant), report md5 855f24e406e6089fe03de7ab7ce9331d — moved LEGITIMATELY from #11's 251c795b8a7f54aa4decd84a38b1b159 because rendered prose changed — and idempotent across 3 consecutive runs. NEXT: #4 (C9 삼형 shadowing), coupled with client-visible item 3 (육합 dead code), because 巳申 is simultaneously 六合 and 六破 and 육합 is tested first in the strict first-match chain, so fixing 육합 alone flips 4 of C9's 5 locks rather than 5.

Carried #4 + client-visible item 3 (육합 dead code) — MEASUREMENT COMPLETE 2026-09-14, **no fix applied and no decision taken**; the tree is left DIRTY at a measured variant. The measurement phase asked the only question that could be asked before committing to a chain order: does the 삼형 priority position actually change any engine output? Four variants of compat.py were built and measured end-to-end via /tmp/variant.py {pristine|A|A-mid|A-high} (pristine backup at /tmp/compat.py.orig, verified INTACT at md5 ff719d82d4a8b617314bfa1de5d67adc, so the tree is fully recoverable). A = the item-3 arity fix only (_branch_pair_lookup widened from List[Tuple[str,str]] + 2-tuple membership to Sequence[Tuple[str,...]] + row[:2] matching, which is what makes the 육합 branch reachable at compat.py:310/:407/:1295); A-mid = arity + 삼형 slotted after 육충 (육합→육충→삼형→육해→육파→반합); A-high = arity + 삼형 hoisted above 육충 (육합→삼형→육충→육해→육파→반합). Measured: pristine (ff719d82…) 880 passed / 15 xfailed / 0 failed, CLI 194 (189 PASS / 5 INTERP / 0 FAIL), 0/5 C9 rows flipped, mahesh-vp 64/Mixed/daybranch 2 — the campaign-close baseline, reproduced. **A** (446e7a91…) 5 failed / 875 passed / 13 xfailed / 2 xpassed, CLI 194 (188/5/**1 FAIL**), 2 XPASS, 0/5 flipped, mahesh-vp 68/Strong/daybranch 6. **A-mid** (07534b13…) same 5 failed / 875 / 11 xfailed / 4 xpassed, CLI 194 (188/5/1), 4 XPASS, flips 寅巳 and 未戌. **A-high** (f2ba2d2e…) same 5 failed / 875 / 9 xfailed / 6 xpassed, CLI 194 (188/5/1), 6 XPASS, flips 丑未 + 寅巳 + 寅申 + 未戌. THE DECISIVE RESULT, and it is stronger than the rendered-report comparison I first reached for: dumping all **27** compat fixtures under each variant (/tmp/dump_all.py → composite / band / daybranch, via _compat_pair + compat_score directly because run_compat_fixture returns {'id','status','mismatches','detail'} and NO 'subsystems' map) shows **A vs A-mid vs A-high differ on ZERO rows** — only the label line differs — and pristine→A moves exactly **ONE** row in the entire corpus, compat-mahesh-vp (64/Mixed/daybranch 2 → 68/Strong/daybranch 6). So the 삼형 chain position is a **NO-OP on everything the engine computes**: it changes no published anchor, no fixture expectation, and no client-visible value; the only thing it moves is pytest xfail-marker bookkeeping (2 → 4 → 6 XPASS, monotonically, as each variant un-shadows more pairs). **The arity fix alone is the entire behavioural payload of client-visible item 3** — the 삼형 reorder is decorative at fixture scale, which is exactly the kind of claim that has to be measured rather than assumed, because C9's 5-shadowed-pair lock LOOKS like it is measuring five real behaviours and is in fact measuring one real behaviour (巳申, which only moves because 육합 now shadows it) plus four bookkeeping entries. ANCHORS, identical across A / A-mid / A-high (published verdicts cannot move): compat-harish-manvitha 70/Strong/daybranch −6/PASS; compat-harish-vinothini-pillars 45/Mixed/−4/INTERPRETATION; compat-pawan-sruthi-stale 60/Mixed/2/INTERPRETATION; compat-mahesh-vp 68/Strong/**6**/**FAIL**; compat-harish-manvitha-override-b-earth 74/Strong/−6/PASS — i.e. the two published anchors (70/Strong and 74/Strong) are untouched by the arity fix. THE FIVE FAILING TESTS are the identical five under A, A-mid and A-high, which is why the choice of chain order does not change the fix's blast radius: three are plain RE-PINS (test_compat_daybranch_returns_exact_demo_score — _MA_VP_SCORES["daybranch"] 2→6; test_compat_order_independence_band_flipping_instance — band Mixed→Strong; test_compat_fixture[compat-mahesh-vp] — 64/Mixed→68/Strong), one is the defect pin carrying the 巳申 question (test_punishment_pairs_reach_their_step), and one is Guard 3 (test_guard_lookup_is_exhaustively_false_for_six_combinations) whose own docstring declares its failure the intended delete-the-marker signal — so the failing set is *entirely* expected lockstep, with no surprise regression, and all three re-pins trace to the single mahesh-vp row. ANALYTICAL FINDING driving the pending decision: **C-high is classically indefensible on documented grounds, not merely unsourced.** Hoisting 삼형 above 육충 makes 丑未 and 寅申 lose their knowledge/11-gunghap.md §B4 label ("육충 — strongest RED FLAG") and drop −5 → −2, contradicting B4 and B8's consensus that 일지 육충 is the strongest unfavorable signal; C-mid at least preserves 육충's documented primacy and displaces only 해/파, but KB11 ranks 형 against nothing — the only textual hint is 11-gunghap.md:1052 (연해자평 婚姻宜避刑沖) which names 형 and 충 together and does NOT rank them. So EVERY candidate position for 삼형 encodes a priority with no knowledge/ source, and C-high actively contradicts one. RECOMMENDED (to be put to the user, NOT decided here): **C-none** — document 삼형's unsourced status and re-pin C9 as a documented_interpretation scope limit, mirroring the accepted #11 precedent (name the scope and pin it rather than silently widening the model). The two questions to ask are **(1) 육합/B1** — (A) arity fix only vs (B) arity + the 11-gunghap.md:137 halving caveat read as its OWN worked examples require, with the narrow 巳申-only reading flagged unsupported by the text; and **(2) #4 삼형** — C-none (recommended) vs C-mid vs C-high. SEPARATE, LARGER FINDING — a KNOWLEDGE-BASE INTEGRITY GAP, not an engine bug: branch **삼형 (三刑) is documented NOWHERE in knowledge/** (zero grep hits for 삼형|三刑|寅巳申|丑戌未 across the whole directory) even though the engine implements it and weights it at −2. 자형 (自刑) *is* documented (02-branches.md:96, 07-special-formations.md:320, 00-glossary.md:79,189); 07-special-formations.md's Part 3 relation table lists 합 · 충 · 자형 · 해 · 파 and OMITS 삼형; 11-gunghap.md:323's 상형 −4 is the Nayin table (§C, 서전구미록), not branch 삼형; and the W6 authoritative research doc docs/research/2026-09-validation-compat-career.md §B enumerates B1 육합 / B2 삼합·반합 / B3 방합 / B4 육충 / B5 자형 / B6 해·파 and never lists 삼형. The engine's 삼형 table is CORRECT — nothing in knowledge/ sanctions its use, its weight, or its priority. Surface alongside #10. CARRIED DETAILS for whoever lands #4: (a) **L3 is a mis-constructed, permanently-unsatisfiable lock** — tests/validation/test_val_compat.py:331-342 `_pillars_chart` hardcodes ("year","甲","子") for BOTH charts so the lock cannot distinguish the two partners; it did NOT XPASS under any variant, so against a fixed engine it will read as an unfixed defect and must be repaired or deleted as part of landing #4; (b) the compat.py:428 반합 suppression guard stays LATENT after the arity fix (vacuous either way), and the secondary cross-chart loop skips any branch equal to the day branch (`if branch_b == day_b: continue`), so only 5 of 6 nominal cross pairs are evaluated when a chart repeats its day branch; (c) the **B1 caveat reading** — 11-gunghap.md:137 ("if 해 or 형 also exists in the cross, weight is halved") is NOT a same-pair rule and NOT 巳申-only, because its own worked examples (자축합 disturbed by 자미해; 인신형) are cross-level CO-OCCURRENCES; implementing it requires turning the strict first-match chain into an INVENTORY SCAN, a real behaviour change unlike the arity fix. DIRTY-TREE STATE AS LEFT (deliberate — the user declined the restore and asked to close the session, so the measurement artifacts are preserved for the next window): src/saju_engine/compat.py is at variant **A-high**, md5 f2ba2d2e6d2c7fcfb9fa40c3a79f7407 (pristine ff719d82d4a8b617314bfa1de5d67adc), with the arity fix visible at :272 and 삼형 hoisted at :312 — so the tree currently carries the one behavioural change (arity) plus a decorative reorder, and the suite reports 5 failed / 875 passed / 9 xfailed / 6 xpassed against it. docs/audits/2026-09-engine-validation-report.md holds the variant render md5 530fe49cec28e204aff2d23f3ce70b6d (pristine 855f24e406e6089fe03de7ab7ce9331d). EXACT RESTORE: `cp /tmp/compat.py.orig src/saju_engine/compat.py && python3 tools/run_validation.py`, then confirm the report returns to md5 855f24e406e6089fe03de7ab7ce9331d and CLI to 194 (PASS 189, INTERPRETATION 5, FAIL 0) and the suite to 880 passed / 15 xfailed / 0 failed. /tmp/variant.py pristine does the same first step. Measurement harnesses, all intact: /tmp/variant.py (variant writer), /tmp/measure.sh (per-variant md5 + suite + CLI + anchors + 5-pair C9 lock probe), /tmp/anchors.py (5 published anchors), /tmp/dump_all.py (all-27-fixture dump), and the four dumps /tmp/dump_{pristine,A,A-mid,A-high}.txt. NO REPOSITORY FILE OTHER THAN compat.py AND THE RENDERED REPORT WAS TOUCHED IN THIS WINDOW — no test was edited, no fixture re-pinned, no xfail marker deleted, and no knowledge file changed, because the whole point of the phase was to measure the choice before making it.
