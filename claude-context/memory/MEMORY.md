# Memory Index

- [Engine validation campaign 2026-09](engine-validation-campaign-2026-09.md) — spec+Plans 1–6, **CAMPAIGN COMPLETE 2026-09-14**: all six workstreams certified (W1 pillars 17, W2 대운/세운 19, W3 lookups 56, W4 용신 13, W5 조후 30, W6 compat+career 59 = 194 checks / 189 PASS / 5 INTERPRETATION / 0 FAIL, 8 subsystems); post-campaign carried-defect fixes took the suite to **883 passed / 12 xfailed / 0 failed** (CLI still 194/189/5/0, exit 0, stdout md5 9626959a… unchanged and idempotent — it hashes a two-line COUNT summary, while the report file's md5 c697db8f… tracks PROSE, so a stable stdout md5 is not evidence prose didn't change); xfail(strict=False) bug-lock pattern + fix-immune-vs-defect-pin taxonomy; documented_interpretation is a guard not a verdict; inline controller mode (glm subagent corruption); anti-loop browser protocol
- [GTM pivot 2026-09](gtm-pivot-2026-09.md) — English-global/USD pivot; `improvements_issues.md` is the master doc; G1–G3/G6 engine fixes landed; suite 590; testimonials + checkout still open
- [Audit 2026-08 handoff](audit-2026-08-handoff.md) — deep engine audit done; findings in `docs/engine_audit_2026-08-22.md`; Phase 0/1/2 done, only Phase 3 intake hardening remains
- [Climate favorable-element fix 2026-09](climate-favorable-element-2026-09.md) — added 조후 climate-balance cross-check to 용신 engine; fixed a related compat.py scoring/override bug; suite 649; 3 items flagged not fixed (premium_report.py bug, Harish's stale follow-ups, compat-PDF output-path bug)
- [User feedback: scope decisions](user-feedback-scope-decisions.md) — user prefers fixing correctness bugs fully/forward over deferring once they reach paid client deliverables, even beyond an approved plan's scope
- [Open defects triage (13 carried)](saju-open-defects-triage.md) — 2026-09-14: user directed work on items 4–13; **NINE are now fixed** (#5, #6, #8, #9, #11, #12, #13, plus #4 + client-visible item 3 landed together as **(A) + C-none** on the user's verbatim approval "(A) + C-none, go ahead" — the arity fix only, chain ORDER untouched, plus a documented_interpretation scope pin for the unsourced 삼형 position, the same shape as the accepted #11 precedent). **Remaining: #7** (client-report regen — now UNBLOCKED, since #9 and item 3 both landed and mahesh-vp moved 64→68) and two product decisions to **surface, not decide** (#10 조후-vs-억부 gate width; #11's model expansion). Client-visible item 1 is **still open and out of the directed scope** (`premium_report.py:181` `_right_now_callout` and `:1395` `ctx_favorable_phrase` read RAW `candidate_favorable` instead of `yongsin.favorable_element()`, pinned by 2 `xfail(strict=False)` at `tests/validation/test_val_climate.py:228`/`:249`) — it is the last surface in the two-channel class, because #6 already resolved item 2. **(A) verified three ways**: fixture `expected` 68/Strong/6, harness PASS/`expect_match`, and a direct `compat_score()` call giving composite 68/68, band Strong/Strong, daybranch 6/6, zero subsystem mismatches — the +4 is the 육합 intended value at `compat.py:310`, and the 65 band boundary is why one fixture flipped Mixed→Strong rather than merely nudging. The 삼형 chain position measured a **NO-OP on all 27 fixtures** (A vs A-mid vs A-high differ on zero rows), C-high contradicts KB11 §B4, and branch 삼형 is documented **nowhere** in `knowledge/`. The arity fix and C9 are **DECOUPLED** (arity-only flips 0 of C9's 5 rows; the 4-of-5 flip is the 삼형 hoist, not 육합) — C9 stays a fix-immune lock, re-pinned as a documented scope limit, its 5 rows LEFT UNFIXED BY DESIGN. Suite **883 passed / 12 xfailed / 0 failed** (12 = 5 C9 + 2 climate + 5 dynamic documented_interpretation escapes); CLI 194/189/5/0, exit 0, stdout md5 `9626959a…` idempotent, report md5 `c697db8f…`. Fixing a *pinned* defect ripples across four lockstep surfaces (defect pins, fix-immune guards, xfail markers, anchors + `_GATE_HISTORY` + render prose). ⚠️ md5 `compat.py` against `/tmp/compat.py.orig` before measuring anything — the old `ff719d82…` invariant is **EXPIRED** (current `9de82a55…`); the durable check is that the diff touches exactly 3 hunks (line 39 typing import, line 272 signature, lines 274–291 body) and no tables or ladder, and the `/tmp/variant.py` switcher leaves BOTH `compat.py` AND the CLI-rendered report stale, since the report only ever reflects the last CLI run's tree
