---
name: engine-validation-campaign-2026-09
description: "Saju engine validation campaign (2026-09-13) — spec + Plans 1–6, ALL SIX WORKSTREAMS CERTIFIED, CAMPAIGN COMPLETE 2026-09-14: 194 checks / 189 PASS / 5 INTERPRETATION / 0 FAIL across 8 subsystems; 13 loose ends carried PINNED not fixed, and as of 2026-09-14 NINE post-campaign fixes have landed (suite 883 passed / 12 xfailed / 0 failed; CLI 194/189/5/0, exit 0, stdout md5 9626959a… idempotent), leaving #7 + two product decisions; inline controller protocol"
metadata: 
  node_type: memory
  type: project
  originSessionId: 84812262-16a1-449c-a1cf-bc8051f2cc40
  modified: 2026-09-14T14:35:16.550Z
---

Engine-validation campaign running (spec `docs/superpowers/specs/2026-09-13-engine-validation-campaign-design.md`, approved). Validation-only: bugs logged, never fixed in-campaign (sole exception: user-ordered ya-ja-si fix). Continuous execution, no user check-ins between tasks.

**Plan 1 (foundation + W1 pillars) FULLY CLOSED — W1 CERTIFIED 2026-09-13.** Suite 672/0 at gate; 17 pillars fixtures; final review fix wave applied + controller-verified (md5 ac42aeea…). Includes user-directed 야자시 hour-stem bug fix (pillars.py `_hour_stem_day_stem` returns next day's 일간 for eff_hour==23 Korean convention; client reports for 23:00–23:59 births need regen — Pawan (1991-10-03 23:45) + Mahesh (1995-01-19 23:50), deferred to campaign close).

**Plan 2 (W2 대운/세운) FULLY CLOSED — W2 CERTIFIED 2026-09-13.** Final whole-plan review verdict READY (independently verified by controller: suite 684/0, CLI 36/36 PASS idempotent md5 2e71a9a6…, direction rule 11/11). Findings: 0 Critical/Important; M1 (roh-moo-hyun note "역방향" typo) + M2 (辛巳 "양" mislabels ×2) DEFERRED as cheap fixture-note edits on next fixture touch; M3/M5/M6 ACCEPTED; M4 (brace-shorthand evidence paths) DEFERRED to Plan-3 evidence work. T3 was executed inline after 4 corrupted glm-5.3-flash subagent attempts.

**Plan 3 (W3 lookups) at `docs/superpowers/plans/2026-09-13-engine-validation-plan-3-lookups.md` — authored 2026-09-13, executing INLINE (corruption protocol override; /tmp/sdd-val3/ for reports).** W3 scope is the spec row verbatim: **십신 (all 100 stem pairs) + 12운성 (all 120 cells) + 납음오행 (60 jiazi→nayin mappings + 30 elements) + 공망 (6 旬 blocks)** — NOT 신살/합충 (those are elsewhere in the spec, not W3). 5 tasks. Key design facts:
- New "lookups" subsystem; runner does NOT call compute_chart — dispatches on `input.lookup` ∈ {ten_god, twelve_stage, nayin, xunkong} → `lookup.ten_god`/`lookup.twelve_stage`, `nayin.JIAZI_TO_NAYIN`/`nayin_element_of`, `stars._xun_kong` (private, documented-acceptable).
- 56 entries (10 ten-god rows × 10 stems, 10 twelve-stage rows × 12 branches, 30 nayin categories covering all 60 jiazi exactly, 6 旬 blocks checked across all 10 pillars each). Row-level granularity; coverage test `test_coverage_exhaustive` pins it (intentionally failing during T1/T2 — known intermediate state, not a defect).
- 空亡 non-circular: fixture carries classical rule (甲子旬→戌亥, 甲戌→申酉, 甲申→午未, 甲午→辰巳, 甲辰→寅卯, 甲寅→子丑) from knowledge/00-glossary.md:34; engine compared against it, not vice versa.
- Nayin 900-cell pair table STAYS element-grammar-fallback (knowledge/11 §C: pair-specific 서전구미록 table never located; sajplus.tistory.com/2017 only 5×5 grid) — Plan 3 asserts the fallback grammar, never replaces it.
- Ground-truth tables transcribed verbatim into the plan: ten-god 10×10 from knowledge/05:44–53; 12운성 all 10 tables from knowledge/06:52–122; nayin 30 pairs from knowledge/11:284–313.
- T4 = external web cross-check (2–3 independent Korean refs per family, WebSearch/WebFetch only, NO browser automation — table data, not charts) + `_GATE_HISTORY` W3 append + gate line update + certification.
- Suite trajectory: 684 → +10 (T1, 694) → +10 (T2, 704) → +36 (T3, 740); CLI checks 36 → 92.

**Protocol (binding):** no-git repo — full-suite pytest is each task's gate; ledger `.superpowers/sdd/progress.md`; reports per plan dir. Inline controller mode BINDING for execution (4 consecutive corrupted glm-5.3-flash subagent attempts; plan-header pattern kept nominally). Anti-loop browser protocol: stable GET URLs, never re-fill forms, 2-strike cap per site, pre-plan all birth data, ~8-submission budget. Controller must independently re-run pytest/verify claims — subagent output not taken at face value.

**Plan 3 (W3 lookups) FULLY CLOSED — W3 CERTIFIED 2026-09-13.** 5 tasks inline; reports in /tmp/sdd-val3/ (task-1..4-report.md + plan-3-final-review.md). Final state: 56 lookups fixtures ≈ 286 cells (100 십신 + 120 12운성 + 60 jiazi→nayin + 30 nayin elements + 60 공망), all expect_match, 56/56 PASS. T4 external cross-check clean for all 4 families (≥2 Korean refs each, 0 discrepancies, external {citation,url} refs appended to all 56 fixtures); 음간 역행 school debate documented as traditional-rule ground truth. Plan-2 deferrals M1/M2/M4 closed in T4 (daeun.json note-only: 역방향→순행, 辛巳 (양)→(음) ×2, 4 brace-shorthand evidence paths literalized). Review READY, 0 Critical/Important/Minor open (5 Informational documented). Gates at close: suite 741/0, CLI 92 PASS ×2 idempotent md5 52d2b2947bc6139f289201319897cc3f, fixture counts 17+19+56=92 match _GATE_HISTORY; read-only surfaces (lookup.py/nayin.py/stars.py) untouched per mtime. Report: docs/audits/2026-09-engine-validation-report.md with W3 section (line 65) + certification lines 130/132.

**Plan 4 (W4 yongsin) AUTHORED 2026-09-13 — `docs/superpowers/plans/2026-09-13-engine-validation-plan-4-yongsin.md` — executing INLINE (corruption protocol; reports /tmp/sdd-val4/).** 5 tasks: T1 harness (check_yongsin/run_yongsin_fixture + _YONGSIN_EXTRA_INPUT {"override","reader_override_favorable"} + registry + 5 layer-A fixtures), T2 corpus+override entries (13 total; 3 documented_interpretation: sruthi/pawan/mahesh published diverge or stale ya-ja-si), T3 research doc docs/research/2026-09-validation-yongsin.md (WebSearch/WebFetch only), T4 external cross-check + _GATE_HISTORY W4 row + gate→Plan 5 + certification, T5 final review. Key probe facts (grounding 2026-09-13, all verified): 3 synthetic weak charts (weak-cold-agree 1984-11-10 03:00 → Fire weak-dm-support cold agree; weak-cold-priority 1980-01-20 09:00 → Metal cold DISagree 억부 priority; weak-temperate 1984-08-20 09:00 → Wood temperate); reader-override path returns DEFAULT climate fields (band "temperate", clim None, agrees None) regardless of chart; reader-confirmed supporting = strict-classical generator of override (Earth→Fire, Water→Metal); all 5 method labels covered. Corpus published verdicts: rm strong/Water/Wood AGREE; gurumoorthy strong/Metal AGREE on element; harish balanced→Water climate AGREE; vishnu-priya balanced→Water AGREE; sruthi published Earth (reader) vs engine Fire climate-balanced → DIVERGENT; pawan (Water, stale chart) + mahesh (Weak/Earth/Metal/Fire-avoid, stale chart, mahesh-report.md:42-45) documented_interpretation. Suite trajectory plan: 741 → +7 (T1) → +8 more entries (T2).

**Plan 4 (W4 yongsin) FULLY CLOSED — W4 CERTIFIED 2026-09-13.** 5 tasks inline; reports /tmp/sdd-val4/ (task-1..4 + plan-4-final-review.md). Final state: 13 yongsin fixtures (3 synthetic weak-cold merge, 4 published AGREE [rm/gurumoorthy/harish/vishnu-priya], 3 documented_interpretation DIVERGE [sruthi Earth-vs-Fire, pawan Water-vs-Wood, mahesh Earth-vs-Fire — reader-argued borderline balanced], 3 override-channel), all 5 method labels covered. T3 research doc docs/research/2026-09-validation-yongsin.md (6 sources, 4 verbatim Korean quotes; 두루미사주 conditional = closest match to engine band rule; no source contradicts precedence). T4: 3 rule families × ≥2 sources, external refs appended to 13/13 sources; report edits ALL via render_report (W4 prose block + optional `note` field on _GATE_HISTORY rows — reusable mechanism). Gates at close: suite 753/0 (753 passed + 3 xfailed), CLI 105 checks (PASS 102, INTERP 3, FAIL 0) idempotent md5 1d3f81ccc699bd9abdbeba1531bc2752; registry order pillars/daeun/sewoon/lookups/yongsin. Gaps logged not fixed: full 120-cell 궁통보감 table, 辰戌丑未 banding, 종격.

**Plan 5 (W5 climate/조후) FULLY CLOSED — W5 CERTIFIED 2026-09-13.** 5 tasks inline; reports /tmp/sdd-val5/ (task-1..4 + plan-5-final-review.md). `climate` subsystem = 30 fixtures in two probe layers dispatched on `input.probe`: 16 `band` (12 month branches + 4 defensive edges `""`/`"Z"`/`"甲"`/`"子時"` — the last proving an hour label is never read as the 子 month) all-chart-free exact-key guard, and 14 `merge` (chart-based unknown-key guard) covering all 9 band × verdict-class cells. `check_climate`/`check_climate_merge`/`run_climate_fixture` + `_CLIMATE_BAND/_MERGE/_ALLOWED_INPUT`; unknown **or missing** `probe` raises (no silent default). 3 rows keep `documented_interpretation` as a published-divergence guard. T3 research doc `docs/research/2026-09-validation-climate.md` (401 lines, 16 unique URLs / 7 domains). T4 landed the **non-strict-xfail bug-lock pattern** (new this plan): 2 `@pytest.mark.xfail(strict=False)` pins on `premium_report.py` `_right_now_callout ~L181` + `ctx_favorable_phrase ~L1395` reading raw `candidate_favorable` instead of the resolved element, with 2 passing guards (input-schema expressibility + premise guard so the locks can't go vacuous); `premium_report.py` untouched (grep still exactly 2). `strict=False` is deliberate — a later sanctioned fix turns them XPASS (delete-the-marker signal) rather than failing the build.

**W5's material findings (both = plan prediction refuted by measurement, resolved in favour of the measurement):**
- **`documented_interpretation` is a safety GUARD, not a verdict.** `INTERPRETATION` is reachable only on a mismatch AND that status (`validation.py:74-79`, mirroring `check_pillars`). The 3 guarded climate rows assert the *engine's* values, so they match and legally PASS → W5 renders **30 PASS / 0 INTERPRETATION** (plan predicted 27/3). The CLI's 3 INTERPRETATION rows remain **W4's**. Corrected at execution: `_GATE_HISTORY` `passed: 27`→**30**, CLI `PASS 129`→**132**, scorecard `30|27|3|0`→`30|30|0|0`, and the cert-checklist box rewritten (not the frozen fixture).
- **조후 vs 억부 has NO unconditional ranking.** The plan's premise (classical sources rank 조후 above 억부 always, making the engine a deviator) is **unsupported** — sources say *"조후와 부억에는 명식을 떠난 고정 순서가 없습니다"* / priority flips on whether the chart is too cold/hot. The engine's 억부-first rule is one defensible school position; the real difference is a **narrower gate** (engine keys on the strength verdict, sources on climate extremeness). Widening would move the 8 `climate_agrees: false` published outputs → **campaign-close product decision**, engine UNCHANGED.
- 辰戌丑未: engine diverges only on **辰/戌** (temperate/None vs 濕→火 / 燥→水); 丑/未 agree by **convergent validity** (source pairs 辰丑/戌未 by damp-dry, engine pairs 辰戌/丑未 by quartet, both land on the source element). `knowledge/17-climate-method.md:51-52` carries an **inverted wording defect** for 辰/未 (logged, not fixed — `climate.py:43-47` never reads the aside).

Gates at close: suite **791 passed / 5 xfailed / 0 failed** (5 = 3 W4 yongsin + 2 T4 locks); CLI **135 checks (PASS 132, INTERPRETATION 3, FAIL 0)** exit 0, stdout md5 **`b2ca53714fcaf20107c63ac42465cf76`** identical across three runs; report md5 `b1a1f7ac0085aaf87b3bcb661f1dd56d`; campaign total **132/135 across 6 subsystems** (W1 17, W2 19, W3 56, W4 13, W5 30, W6 0). Review READY, 0 Critical / 0 Important, 11 Minor/Informational documented. Registry order now pillars/daeun/sewoon/lookups/yongsin/climate.

**Next after Plan 5:** author **Plan 6 (W6 compat / spouse + career)** — the campaign's FINAL workstream, then campaign close-out. Deferred to campaign close: prune _CHECKERS if never consumed; regen Pawan (1991-10-03 23:45) + Mahesh (1995-01-19 23:50) client reports/compat pair + rebuild PDFs (야자시 fix — both marked STALE CHART in W5 fixture `source` strings); the live `_right_now_callout`/`ctx_favorable_phrase` bug (spec Out of Scope; now pinned by the 2 locks); stale Harish `career.md`/`land-workshop-business.md`/`luck-timeline.md`; `md_to_saju_compat_pdf.py --output` path bug; the 조후-gate-width product decision above; the knowledge-file wording defect above.

**Plan 6 (W6 compat / spouse + career) — the campaign's FINAL workstream — authored at `docs/superpowers/plans/2026-09-13-engine-validation-plan-6-compat-career.md`; executing INLINE (reports `/tmp/sdd-val6/`).** 5 tasks: T1 harness+layer-A, T2 corpus+override+`score`-probe, T3 research doc, T4 external cross-check + `_GATE_HISTORY` W6 row + certification, T5 final review. **No successor gate — T5 close ends the campaign.**

**Plan 6 T1: COMPLETE (2026-09-14).** compat subsystem registered; 17 layer-A fixtures (10 `band` chart-free probes — the 4-band ladder with all three boundaries 45/65/80 straddled + 2 out-of-range defensive edges; 7 `subsystem` probes on the *published* harish × manvitha pillars = exactly A6's pillars-only-reproducible set, the other 4 [yongshin/ilju_pair/combined_elements/daeun_sync] declared out-of-scope in every entry's notes). All 17 `expect_match`. `_CHECKERS` wired for the first time — 7/7 across `_SUBSYSTEM_ORDER`/`_WORKSTREAM`/`_RUNNERS`/`_CHECKERS` — by renaming the registered band checker `check_compat_band`→`check_compat` (W5 precedent: **the registered checker is unsuffixed, the deliberately-unregistered probes stay suffixed, so the name encodes registry membership**). Behaviour-neutrality PROVEN, not assumed: CLI stdout md5 byte-identical to the pre-rename baseline, confirming `_CHECKERS` is dead code (only `_RUNNERS` is consumed; pruning still deferred to campaign close). Gates: suite **824 passed / 13 xfailed / 0 failed**; CLI **152 checks (PASS 149, INTERPRETATION 3, FAIL 0)** exit 0, stdout md5 `a22f946613ef38901403ad13e4592cfe` idempotent across two runs; report md5 `5d3be0855f6391ee95fcb4fe57292e89`; registry order `pillars/daeun/sewoon/lookups/yongsin/climate/compat`; CLI arithmetic 132+17=149 ✓; INTERPRETATION rows stay W4's. Report `/tmp/sdd-val6/task-1-report.md`.

**T1's NEW FINDING — the 육합 defect (NOT in the spec's 3 known bugs; logged, NOT fixed per validation-only).** `lookup.SIX_COMBINATIONS` is `List[Tuple[str,str,str]]` (each row carries the resulting element, e.g. `('子','丑','Earth')`) but `compat._branch_pair_lookup` tests **2-tuple** membership (`(b1,b2) in table`), so the 육합 branch is unreachable dead code at all four call sites: `compat.py:310` `_cross_branch_score` (intended +4), `:407` `compat_daybranch` (spouse-palace +20 — the engine's own narrative calls it "the strongest favorable indicator in classical 궁합" yet reports such a pair as having "no direct 합/충", a **false statement in a paid deliverable**), `:1295` `compat_year_branch` (띠 +3), and `:428` (반합 suppression guard — vacuous but LATENT ONLY: no 六合 pair shares a 삼합 frame). Inverted-result nuance: 寅亥 and 巳申 are simultaneously 六合 *and* 六破, so they score **−1** (a penalty where classical 궁합 prizes a combination) rather than 0. The other three relation tables are 2-tuples and work, so the defect is table-specific and the fix is one bounded line. 3 `xfail(strict=False)` locks + 3 passing guards.

**C8 — a THIRD, independent compat defect: `compat_score(a,b)` is not symmetric.** `nayin` is direction-dependent. Two measured instances: band-FLIPPING (Mahesh×VP fwd 64/Mixed vs rev 66/Strong, nayin +3 vs +5) and band-STABLE (Harish×Manvitha fwd 70/Strong vs rev 68/Strong, sole differing sub-system `nayin` −3 vs −5). Lock + 2 guards appended to `tests/test_compat.py` in T2 Step 7.

**C9 (삼형 shadowing) is a DIFFERENT defect from 육합 and the two are independent.** `_cross_branch_score`'s priority chain is 육합:310 → 육충:312 → 육해:314 → 육파:316 → 삼형:318 → 반합:321 → `(0,None)`:324, so 삼형 sits below 충/해/파 and five 三刑-frame members report the wrong relation. Fix simulation: 육합-only flips **0/5** of the C9 locks, C9-only **5/5**, both **4/5** — the 巳申 row is simultaneously 六合 and 六破, so once 육합 is fixed that one C9 expectation becomes unsatisfiable. Documented in a code comment so whoever lands both expects **4 XPASS + one permanently-xfailing 巳申 row** and decides 巳申 priority on classical grounds.

**TWO DISTINCT GUARD PHILOSOPHIES — a reusable testing technique from this campaign (do not conflate them).** *Fix-immune guards* (T1's 육합 block) assert behaviour a sanctioned fix will **NOT** change, so the lock cannot go vacuous when the fix lands (e.g. guard 3 asserts `_branch_pair_lookup` is exhaustively False for `SIX_COMBINATIONS` **as written** — the inverse premise, which fails precisely when the behaviour changes). *Defect pins* (T2's C8 block) assert the **measured defect values**, so a sanctioned fix makes the lock XPASS **and both guards FAIL simultaneously** — the intended paired-update signal — and a *partial* fix (one that moves the composite without making nayin symmetric) is caught rather than passing silently. The plan wrote the C8 guards as if they were fix-immune; I implemented them verbatim but labelled them defect pins in a comment block. **Recorded design decision, not a silent absorption.**

**Plan 6 T2: FULLY CLOSED (2026-09-14).** Work done: (a) **Edit A — a real implementation gap FIXED:** `check_compat_score` dropped the `favorable_element_a`/`favorable_element_b` args while `_COMPAT_SCORE_INPUT` admitted them (product-visible; band-flipping in the `a` direction, band-stable in `b`); (b) T1's two subsystem tests scoped to `charts == "pillars"` via a new `_pillars_only_subsystem_entries()` helper, resolving a **Step-6-vs-T1 plan contradiction** (Step 6 asserts all 11 `WEIGHT` keys while T1's test asserted exactly the 7-element pillars-only set over the same unfiltered set); (c) 10 Layer B entries appended to `compat.json` (17→27) byte-safely, all 24 `a_*`/`b_*` keys explicit → **8 PASS / 2 INTERPRETATION**; (d) Step 1 + Step 6 module tests added, both **sensitivity-proven non-vacuous** (by value — perturbing a subsystem or the composite FAILs; by shape — pillars-only covers exactly 7, birth adds exactly the complementary 4, overlap none); (e) Step 7 C8 lock + 2 guards appended; Step 8 measured **3 passed, 53 deselected, 1 xfailed** (plan predicted 2 passed). Module now collects **57 tests**; 52 passed / 6 xfailed.

**The 10 Layer B entries:** `compat-harish-manvitha` (the **B2 anchor**, zero overrides, 70/Strong with all 11 sub-systems byte-exact: daystem_combo 0, daybranch −6, nayin −3, yongshin 9, ilju_pair 5, combined_elements 9, tengod_cross 10, daeun_sync 1, compat_stars 0, yin_yang 2, year_branch 2); `compat-harish-vinothini-pillars` (57/Mixed **documented_interpretation**); `compat-pawan-sruthi-stale` (55/Mixed, documented_interpretation); `compat-manvitha-harish-reversed` (68/Strong, subsystems `{nayin: −5}`); `compat-mahesh-vp` (64/Mixed); `compat-harish-manvitha-override-b-earth` (74/Strong, yongshin 12, ilju_pair 6, combined_elements 11); + 4 `charts="birth"` `subsystem` probes (yongshin 9, ilju_pair 5, combined_elements 9, daeun_sync 1). **The override perturbs `combined_elements` too** — beyond yongshin/ilju_pair: `b=Earth` 9→11, `a=Fire` 9→7.

**T2's 7 findings —** (1) Plan Steps 2–3 were already satisfied (the `charts="birth"` branch and per-subsystem comparison loop predate T2). (2) The override-forwarding gap above (FIXED). (3) The Step-6-vs-T1 contradiction (RESOLVED by scoping). (4) **THIRD BLOCKING DEFECT — the vinothini fixture was mislabeled:** measured 43/Challenging for `charts="pillars"`, not the planned 49/Mixed (that figure was full/PO); the published 57/Mixed is full/full. Applied as `documented_interpretation` + notes recording 43, the mislabeled 49, the five-row collapse table, and the **unrecoverability of Vinothini's birth data**. (5) **NEW — `compat_stars` is only *vacuously* pillars-only reproducible:** `compat.py:1153` reads `a.stars or {}` (full-pipeline attributes), so a pillars-only chart scores 0 where the published table records **−2** — making the vinothini collapse table **five** rows, not the four A6 predicted. Logged, not fixed. (6) The plan's Step-1 literal fails T1's own guard (three missing `a_*` keys) — the corpus entry states the same claim in the accepted shape. (7) The fixture file is byte-reproducible (`json.dumps(data, indent=2, ensure_ascii=False) + "\n"`, proved at 13303 bytes).

**T2 also found a FALSE-FRIEND TEST (weak-test finding, logged).** `tests/test_compat.py:352` `test_compat_daybranch_asymmetric_scoring` names reverse-direction detection in its name *and* docstring but its body asserts only `-30 <= sub.score <= 30` plus non-crash — a bounds/smoke test that would pass for a constant function. It is (i) why Step 8 measured 3 passed rather than the plan's 2 (a third pre-existing test matches the `-k "symmetric or order_dependence"` selector), and (ii) a genuine coverage gap. It does **not** overlap C8 (it exercises `compat_daybranch`'s *intended* directional cross-chart read, a different axis from C8's *unintended* `nayin` direction-dependence).

**T2 close-out (all independently re-verified by the controller):** (i) the C8 lock's **non-vacuity** proved via `--runxfail` — a genuine `AssertionError: forward 64 vs reverse 66` with the intended message (NOT an ERROR, which `strict=False` would count as satisfied-xfail); (ii) the **Step 9 full-suite gate** MEASURED — **836 passed / 16 xfailed / 0 failed** in 30.96s, and again 31.06s on the post-close controller re-run (counts stable). The plan's 11 / carried 14 / module-inferred 15 were all wrong: **`pytest.xfail()` (the `documented_interpretation` escape) is a THIRD xfail source**, distinct from the two `xfail(strict=False)` marker families, so a marker-only count always undercounts by the number of doc'd-interpretation divergences — the 6th instance of the authoring-time miscount class (Plans 2, 4, 5, 6 ×2). Reconciliation: 824+8+2+2 passed / 13+2+1 xfailed (10 Layer B fixtures = 8+2, Step 1+6 module tests = +2, C8 lock+2 guards = +2/+1). (iii) `/tmp/sdd-val6/task-2-report.md` written (verified on disk, 16734 bytes, all numbers correct — 162/157/5/0, not 152); (iv) **prose** T2 line appended to `.superpowers/sdd/progress.md` (ledger carries 0 checkbox lines — 5th instance of the plan-assumes-a-convention class); (v) `test_val_compat.py:3` stale docstring ("17 layer-A" → "27 entries (17 layer-A + 10 layer-B)") FIXED, module re-verified 36 passed / 5 xfailed. **Final T2 gates: suite 836/16/0; CLI 162 checks (PASS 157, INTERPRETATION 5, FAIL 0)** exit 0, stdout md5 `7047a056ad7879e80fb94d925f8adde7` byte-identical across two runs (reconciled 152+10, 149+8 PASS, 3+2 INTERP — the +2 are the two documented_interpretation rows, confirming guard-not-verdict semantics render INTERPRETATION in the CLI); report md5 `9854d2e1e5fdd3e1a743f754b8a1c89a`; fixture corpus 27; registry order `pillars/daeun/sewoon/lookups/yongsin/climate/compat`.

**Carried, non-blocking:** harish's longitude is `76.33` in `tests/validation/fixtures/sewoon.json` but `79.42` in `yongshin.json`/`climate.json`/the Plan 6 anchors (`79.42` reproduces the published pillars); `_CHECKERS` pruning deferred to campaign close; `test_val_compat.py`'s docstring FIXED at T2 close (now 27).

**Plan 6 T3 (career subsystem) COMPLETE (2026-09-14).** `check_career`/`run_career_fixture`/`_CAREER_*_INPUT`; `career` registered (registry order `pillars/daeun/sewoon/lookups/yongsin/climate/compat/career`); `tests/validation/fixtures/career.json` = **32 entries (30 `domain_map` + 2 `tiers`)**, 5 `documented_interpretation`; `tests/validation/test_val_career.py`. Career tier ladder is `i<4`/`i<7`/else and the **Possible tier is STRUCTURALLY unreachable** (pool 5×6=30 but only 6 candidates → max index 5; asserted at `test_val_career.py:175`), which also pins the **KB12:130-152 Day-Master-keyed contradiction**. Second live instance of the premium_report bug class: `report_data._career_why`/`_career_tiers` read the **stale raw favorable field** instead of `favorable_element()`, contradicting the engine's own resolved 용신 on every career row — locked by 1 xfail + 3 guards in `tests/test_report_data.py` (Guard 1 = byte-exact stale string pin, Guard 2 = `favorable_element(HARISH).element == "Water"` + `method == "climate-balanced"`).

**Plan 6 T4 (external cross-check + certification) COMPLETE (2026-09-14).** `docs/research/2026-09-validation-compat-career.md` (685 lines, md5 `e5edf25e63b32ecd13021b5faba2e6fb`) is the doc every fixture citation is bound to → provenance chain **fixture → research doc → external source**. All **59/59** W6 fixtures carry `source["external"] = {citation, url}` + an inline ref, 0 malformed; the stamper **refuses to double-apply** (idempotent-by-refusal). `_GATE_HISTORY` W6 row (checks 59 / passed 59) + W6 prose block in `render_report()` (keyed to `career` — last in `_SUBSYSTEM_ORDER` — because W6 is the only workstream spanning TWO subsystems, so a per-subsystem `if` would fire twice or land between the tables) + final gate line → **"Campaign COMPLETE — all six workstreams (W1-W6) certified; no successor gate."** Reversibility proven by **writing the Step-2 transform's INVERSE and closing the round trip byte-for-byte** (no VCS diff exists): compat.json 50594→31674 B, career.json 40424→18432 B, every non-`source` field identical.

**Plan 6 T5 (final whole-plan review) COMPLETE — CAMPAIGN CLOSED.** Report `/tmp/sdd-val6/plan-6-final-review.md`; verdict **READY, 0 Critical / 0 Important**, 6 Minor + 4 Informational. Every gate figure **independently re-run by the controller** at the FINAL state (post-ledger-append), not transcribed: suite **878 passed / 17 xfailed / 0 failed**; CLI exit 0, stdout md5 **`9626959ad9b18f658fa49adb1b3ebeee`**, report md5 **`7135b025521e276cfec4e795be6bfa94`**, stderr 0 B, stdout AND report byte-identical across runs; `checks: 194 (PASS 189, INTERPRETATION 5, FAIL 0)`; `find src -name '*.py' -newermt '2026-09-14 00:00:00'` → **exactly one file (validation.py)** = engine untouched. **Cross-surface semantics (new, reusable):** the `_GATE_HISTORY` row's `passed` = the report scorecard's `PASS` **plus** its `INTERPRETATION` column (compat 25+2=27, career 32+0=32) — `documented_interpretation` is a **guard-pass, not a verdict-fail**, so the two surfaces use different words for the same fact. **Campaign delta:** 791→878 passed (+87), 5→17 xfailed (+12, all W6), 135→194 checks (+59 = the W6 fixture count exactly). **Lock inventory at close: 4 families / 10 non-strict xfail markers / 10 guard tests → 15 guard instances, all passing** — C9 삼형 shadowing (fix-immune), 육합 dead code (fix-immune), C8 order-dependence (**defect pin**), report_data stale field (**defect pin**). xfail census reconciles as 12 decorated + 5 dynamic `pytest.xfail()` = 17; **`grep -c '@pytest.mark.xfail'` returns 8 sites and undercounts by 9 — a marker-grep census picks the wrong unit.**

**CAMPAIGN COMPLETE 2026-09-14.** All six workstreams certified: **W1 pillars 17, W2 대운/세운 19, W3 십신/12운성/납음/공망 56, W4 용신 13, W5 climate 30, W6 compat + career 59 = 194 checks** (8 subsystems). Ledger `.superpowers/sdd/progress.md` closed at 384 lines (T5 line + campaign-complete line appended after T4's line, not duplicating it). **No successor gate.**

**Loose ends carried (13, all PINNED not fixed) — client-visible first: (1) `premium_report.py` `_right_now_callout`/`ctx_favorable_phrase` raw-favorable read — LIVE-WRONG in the Pawan/Sruthi client reports; (2) `report_data._career_why`/`_career_tiers` stale-favorable read — second live instance; (3) 육합 dead code (`SIX_COMBINATIONS` is 3-tuple, `_branch_pair_lookup` tests 2-tuple membership → unreachable at compat.py:310 [+4], :407 [spouse-palace +20, where the engine calls it "the strongest favorable indicator in classical 궁합" yet reports "no direct 합/충" — a false statement in a paid deliverable], :1295 [띠 +3], :428 [latent]); (4) C9 삼형 shadowing (5 of 7 형 pairs); (5) C8 asymmetry (70 vs 68, nayin direction-dependent); (6) KB12:130-152 tier-pool contradiction; (7) regen Pawan/Mahesh reports for the 야자시 fix + refresh Harish's stale career/land-workshop/luck-timeline files; (8) `md_to_saju_compat_pdf.py --output` path bug; (9) Harish longitude 76.33 (sewoon.json) vs 79.42 (elsewhere); (10) the 조후-vs-억부 gate-width product decision; (11) 辰/戌 divergence; (12) `knowledge/17-climate-method.md:51-52` wording defect; (13) prune the dead `_CHECKERS` registry.** Per [[user-feedback-scope-decisions]] the user prefers fixing correctness bugs fully/forward once they reach paid client deliverables — items 1–3 qualify, but fixing them needs a new user request (the campaign was validation-only, bugs logged never fixed, sole exception the user-ordered 야자시 fix).

**Post-campaign fix wave (2026-09-14, user-directed on items 4–13 — see [[saju-open-defects-triage]] for the live list).** First five landed, each independently re-measured by the controller, not transcribed: **#6** (`_career_tiers` re-keyed off the Day Master's own family onto the resolved **용신 family followed by the 희신 family** — 12 candidates from 2 families, which is what KB12:130-152 requires; new `_resolved_favorable(chart)` helper; both `_career_why` call sites moved to the display channel, which **also resolved client-visible item 2**, leaving item 1 as the sole surface in the two-channel class), **#8** (`--output` path bug), **#9** (Harish longitude → `79.42` at 3 sites; pillar-neutral), **#12** (unsourced dry/damp parenthetical in `knowledge/17-climate-method.md`), **#13** (dead `_CHECKERS` registry pruned). The #6 fix is the **template**: one helper + two call sites, but **four lockstep surfaces** had to move with it (career.json re-pinned to 12 rows with `possible_tier_reachable: true`; `test_val_career.py` positive pin + docstring; `test_report_data.py` lock retired → positive pin + premise guard; `validation.py` 4 render-prose edits + the `_GATE_HISTORY` W6 note) — and the retired lock's **XPASS was the only signal the marker should go**. **W6 coverage gap 1 is CLOSED** (the `i < 7` **Possible** band is now output-reachable, where the single-family pool left it code-reachable only). Delta: suite **878/17/0 → 879/16/0** = exactly +1 passed / −1 marker; CLI **unchanged** at `194 (PASS 189, INTERPRETATION 5, FAIL 0)` exit 0 with stdout md5 **`9626959ad9b18f658fa49adb1b3ebeee` identical** — the cross-task invariant held, because the stdout carries only the summary line — while the **report md5 legitimately moved** `7135b025521e276cfec4e795be6bfa94` → `ac526882415801fef0628f769f89a600` (rendered career prose changed). xfail census: W6 **10→9** instances, W6 guards still **10**, campaign **17→16** (matching pytest's own `16 xfailed`). Lock inventory at close said the retired item was a *lock*, not a guard — which is why the guard count did not move. **Remaining order at this point: #11 → #5 → #4 + item 3 → #7; #10 is an open product decision to surface, not decide.** ⚠️ SUPERSEDED LATER THE SAME DAY (2026-09-14) — the paragraph above is the snapshot after the FIRST FIVE only. #11, #5 and **#4 + item 3** have since landed as well (nine total; live list in [[saju-open-defects-triage]]). In particular, the coupling this line once asserted — **"#4 + item 3 (coupled: 巳申 is both 六合 and 六破)"** — was **MEASURED AND REFUTED**: the 육합 arity fix alone flips **0 of C9's 5 rows** (it moves 巳申 from 육파 to 육합, which is still not 삼형), so the arity fix and the C9 삼형 question are **DECOUPLED**, and the "a C9 fix alone flips 5/5, C9 + 육합 together flips only 4/5" figure belongs to a separate, unsanctioned 삼형 hoist — do not restate it as a property of the arity fix. The arity fix was landed alone, chain order untouched, with the 삼형 position pinned as a documented scope limit. Current gates: suite **883 passed / 12 xfailed / 0 failed**; CLI 194/189/5/0 exit 0. Live remainder is **#7** (client-report regen, now unblocked) + the two product decisions.

Related: [[climate-favorable-element-2026-09]], [[user-feedback-scope-decisions]], [[saju-open-defects-triage]]